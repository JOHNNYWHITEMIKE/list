version: '3.8'

services:
  git-collaboration:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: git-collaboration
    environment:
      - AGENT_NAME=Git-Collaboration
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
