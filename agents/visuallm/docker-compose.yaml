version: '3.8'

services:
  visuallm:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: visuallm
    environment:
      - AGENT_NAME=Visuallm
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
