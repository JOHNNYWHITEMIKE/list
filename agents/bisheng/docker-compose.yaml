version: '3.8'

services:
  bisheng:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bisheng
    environment:
      - AGENT_NAME=Bisheng
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
