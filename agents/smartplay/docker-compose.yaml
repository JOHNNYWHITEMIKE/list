version: '3.8'

services:
  smartplay:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: smartplay
    environment:
      - AGENT_NAME=Smartplay
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
