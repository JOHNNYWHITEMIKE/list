version: '3.8'

services:
  langchain:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: langchain
    environment:
      - AGENT_NAME=LangChain
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
