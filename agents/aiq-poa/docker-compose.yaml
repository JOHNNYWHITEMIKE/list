version: '3.8'

services:
  aiq-poa:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aiq-poa
    environment:
      - AGENT_NAME=Aiq-Poa
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
