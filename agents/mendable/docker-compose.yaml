version: '3.8'

services:
  mendable:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mendable
    environment:
      - AGENT_NAME=Mendable
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
