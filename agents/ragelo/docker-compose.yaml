version: '3.8'

services:
  ragelo:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ragelo
    environment:
      - AGENT_NAME=Ragelo
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
