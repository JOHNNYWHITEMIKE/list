version: '3.8'

services:
  swarms:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: swarms
    environment:
      - AGENT_NAME=Swarms
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
