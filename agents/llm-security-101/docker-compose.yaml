version: '3.8'

services:
  llm-security-101:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llm-security-101
    environment:
      - AGENT_NAME=Llm-Security-101
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
