version: '3.8'

services:
  spreads:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: spreads
    environment:
      - AGENT_NAME=Spreads
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
