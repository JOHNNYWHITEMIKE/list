version: '3.8'

services:
  chat_agent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: chat_agent
    environment:
      - AGENT_NAME=Chat_Agent
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
