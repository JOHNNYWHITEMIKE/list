version: '3.8'

services:
  init-eliza:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: init-eliza
    environment:
      - AGENT_NAME=Init-Eliza
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
