version: '3.8'

services:
  sale-workflow:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sale-workflow
    environment:
      - AGENT_NAME=Sale-Workflow
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
