version: '3.8'

services:
  fine:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fine
    environment:
      - AGENT_NAME=Fine
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
