version: '3.8'

services:
  introtoaiethics-kaggle:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: introtoaiethics-kaggle
    environment:
      - AGENT_NAME=Introtoaiethics-Kaggle
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
