version: '3.8'

services:
  nexusgpt:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nexusgpt
    environment:
      - AGENT_NAME=NexusGPT
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
