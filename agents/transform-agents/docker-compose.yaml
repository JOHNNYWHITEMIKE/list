version: '3.8'

services:
  transform-agents:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: transform-agents
    environment:
      - AGENT_NAME=Transform-Agents
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
