version: '3.8'

services:
  learnprompt:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: learnprompt
    environment:
      - AGENT_NAME=Learnprompt
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
