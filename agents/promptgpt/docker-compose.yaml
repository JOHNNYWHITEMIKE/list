version: '3.8'

services:
  promptgpt:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: promptgpt
    environment:
      - AGENT_NAME=Promptgpt
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
