version: '3.8'

services:
  adala:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: adala
    environment:
      - AGENT_NAME=Adala
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
