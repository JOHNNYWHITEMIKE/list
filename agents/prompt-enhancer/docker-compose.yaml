version: '3.8'

services:
  prompt-enhancer:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: prompt-enhancer
    environment:
      - AGENT_NAME=Prompt-Enhancer
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
