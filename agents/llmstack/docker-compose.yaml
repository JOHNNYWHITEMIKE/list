version: '3.8'

services:
  llmstack:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llmstack
    environment:
      - AGENT_NAME=Llmstack
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
