version: '3.8'

services:
  configs:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: configs
    environment:
      - AGENT_NAME=Configs
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
