version: '3.8'

services:
  worldai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: worldai
    environment:
      - AGENT_NAME=Worldai
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
