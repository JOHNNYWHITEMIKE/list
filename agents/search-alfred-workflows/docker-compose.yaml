version: '3.8'

services:
  search-alfred-workflows:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: search-alfred-workflows
    environment:
      - AGENT_NAME=Search-Alfred-Workflows
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
