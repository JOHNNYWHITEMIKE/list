version: '3.8'

services:
  anyscale:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: anyscale
    environment:
      - AGENT_NAME=Anyscale
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
