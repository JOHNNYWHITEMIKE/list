version: '3.8'

services:
  crewai-101:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: crewai-101
    environment:
      - AGENT_NAME=Crewai-101
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
