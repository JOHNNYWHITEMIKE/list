version: '3.8'

services:
  pyzeebe:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pyzeebe
    environment:
      - AGENT_NAME=Pyzeebe
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
