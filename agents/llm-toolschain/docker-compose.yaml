version: '3.8'

services:
  llm-toolschain:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llm-toolschain
    environment:
      - AGENT_NAME=Llm-Toolschain
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
