version: '3.8'

services:
  gitwash:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: gitwash
    environment:
      - AGENT_NAME=Gitwash
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
