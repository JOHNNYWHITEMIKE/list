version: '3.8'

services:
  hauth:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: hauth
    environment:
      - AGENT_NAME=Hauth
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
