version: '3.8'

services:
  devopsgpt:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: devopsgpt
    environment:
      - AGENT_NAME=DevOpsGPT
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
