version: '3.8'

services:
  global-workflow:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: global-workflow
    environment:
      - AGENT_NAME=Global-Workflow
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
