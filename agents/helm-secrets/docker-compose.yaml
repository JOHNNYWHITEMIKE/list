version: '3.8'

services:
  helm-secrets:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: helm-secrets
    environment:
      - AGENT_NAME=Helm-Secrets
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
