version: '3.8'

services:
  adtestpro:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: adtestpro
    environment:
      - AGENT_NAME=Adtestpro
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
