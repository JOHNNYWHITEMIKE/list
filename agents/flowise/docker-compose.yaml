version: '3.8'

services:
  flowise:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: flowise
    environment:
      - AGENT_NAME=Flowise
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
