version: '3.8'

services:
  flyte:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: flyte
    environment:
      - AGENT_NAME=Flyte
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
