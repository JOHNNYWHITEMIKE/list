version: '3.8'

services:
  liverecall:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: liverecall
    environment:
      - AGENT_NAME=LiveRecall
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
