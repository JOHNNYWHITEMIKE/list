version: '3.8'

services:
  swe-agent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: swe-agent
    environment:
      - AGENT_NAME=SWE-agent
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
