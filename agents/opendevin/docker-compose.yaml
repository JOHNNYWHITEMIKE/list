version: '3.8'

services:
  opendevin:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: opendevin
    environment:
      - AGENT_NAME=OpenDevin
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
