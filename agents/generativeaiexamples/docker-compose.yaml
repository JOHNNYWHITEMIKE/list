version: '3.8'

services:
  generativeaiexamples:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: generativeaiexamples
    environment:
      - AGENT_NAME=Generativeaiexamples
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
