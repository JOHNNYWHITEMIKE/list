version: '3.8'

services:
  slickflow:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: slickflow
    environment:
      - AGENT_NAME=Slickflow
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
