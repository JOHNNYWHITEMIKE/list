version: '3.8'

services:
  jazzx:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: jazzx
    environment:
      - AGENT_NAME=Jazzx
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
