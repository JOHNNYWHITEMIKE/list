version: '3.8'

services:
  llm-toolbox-suite:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llm-toolbox-suite
    environment:
      - AGENT_NAME=Llm-Toolbox-Suite
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
