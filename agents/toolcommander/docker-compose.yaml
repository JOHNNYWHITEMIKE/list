version: '3.8'

services:
  toolcommander:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: toolcommander
    environment:
      - AGENT_NAME=Toolcommander
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
