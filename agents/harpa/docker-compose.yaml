version: '3.8'

services:
  harpa:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: harpa
    environment:
      - AGENT_NAME=Harpa
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
