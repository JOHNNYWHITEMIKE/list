version: '3.8'

services:
  fetchai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fetchai
    environment:
      - AGENT_NAME=Fetchai
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
