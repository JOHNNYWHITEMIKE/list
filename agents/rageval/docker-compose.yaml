version: '3.8'

services:
  rageval:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rageval
    environment:
      - AGENT_NAME=Rageval
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
