version: '3.8'

services:
  agentools:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agentools
    environment:
      - AGENT_NAME=Agentools
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
