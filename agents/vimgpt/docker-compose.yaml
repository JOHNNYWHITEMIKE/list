version: '3.8'

services:
  vimgpt:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vimgpt
    environment:
      - AGENT_NAME=vimGPT
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
