version: '3.8'

services:
  libraria:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: libraria
    environment:
      - AGENT_NAME=Libraria
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
