version: '3.8'

services:
  gpt-models-by-openai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: gpt-models-by-openai
    environment:
      - AGENT_NAME=GPT models by OpenAI
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
