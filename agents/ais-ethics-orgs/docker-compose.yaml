version: '3.8'

services:
  ais-ethics-orgs:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ais-ethics-orgs
    environment:
      - AGENT_NAME=Ais-Ethics-Orgs
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
