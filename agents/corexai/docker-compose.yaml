version: '3.8'

services:
  corexai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: corexai
    environment:
      - AGENT_NAME=Corexai
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
