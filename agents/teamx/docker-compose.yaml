version: '3.8'

services:
  teamx:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: teamx
    environment:
      - AGENT_NAME=TeamX
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
