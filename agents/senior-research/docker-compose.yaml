version: '3.8'

services:
  senior-research:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: senior-research
    environment:
      - AGENT_NAME=Senior-Research
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
