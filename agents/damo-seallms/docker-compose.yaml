version: '3.8'

services:
  damo-seallms:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: damo-seallms
    environment:
      - AGENT_NAME=Damo-Seallms
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
