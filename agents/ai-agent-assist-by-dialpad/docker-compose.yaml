version: '3.8'

services:
  ai-agent-assist-by-dialpad:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-agent-assist-by-dialpad
    environment:
      - AGENT_NAME=AI Agent Assist by DialPad
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
