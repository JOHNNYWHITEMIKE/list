version: '3.8'

services:
  helicone:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: helicone
    environment:
      - AGENT_NAME=Helicone
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
