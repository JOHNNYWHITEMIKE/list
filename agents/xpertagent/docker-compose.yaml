version: '3.8'

services:
  xpertagent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: xpertagent
    environment:
      - AGENT_NAME=Xpertagent
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
