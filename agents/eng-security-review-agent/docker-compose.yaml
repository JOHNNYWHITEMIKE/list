version: '3.8'

services:
  eng-security-review-agent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: eng-security-review-agent
    environment:
      - AGENT_NAME=Eng-Security-Review-Agent
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
