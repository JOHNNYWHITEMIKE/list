version: '3.8'

services:
  upstreet-core:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: upstreet-core
    environment:
      - AGENT_NAME=Upstreet-Core
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
