version: '3.8'

services:
  notiongpt:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: notiongpt
    environment:
      - AGENT_NAME=Notiongpt
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
