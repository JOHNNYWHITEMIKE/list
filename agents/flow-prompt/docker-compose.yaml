version: '3.8'

services:
  flow-prompt:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: flow-prompt
    environment:
      - AGENT_NAME=Flow-Prompt
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
