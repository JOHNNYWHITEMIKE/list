version: '3.8'

services:
  ceo-agentic-ai-framework:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ceo-agentic-ai-framework
    environment:
      - AGENT_NAME=Ceo-Agentic-Ai-Framework
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
