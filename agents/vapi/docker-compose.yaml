version: '3.8'

services:
  vapi:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vapi
    environment:
      - AGENT_NAME=Vapi
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
