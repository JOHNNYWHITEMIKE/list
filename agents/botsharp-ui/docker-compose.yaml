version: '3.8'

services:
  botsharp-ui:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: botsharp-ui
    environment:
      - AGENT_NAME=Botsharp-Ui
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
