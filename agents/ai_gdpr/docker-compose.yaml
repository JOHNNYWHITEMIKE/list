version: '3.8'

services:
  ai_gdpr:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai_gdpr
    environment:
      - AGENT_NAME=Ai_Gdpr
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
