version: '3.8'

services:
  promptoftheyear:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: promptoftheyear
    environment:
      - AGENT_NAME=Promptoftheyear
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
