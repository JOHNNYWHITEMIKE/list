version: '3.8'

services:
  windowsagentarena:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: windowsagentarena
    environment:
      - AGENT_NAME=Windowsagentarena
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
