version: '3.8'

services:
  ai_apps:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai_apps
    environment:
      - AGENT_NAME=Ai_Apps
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
