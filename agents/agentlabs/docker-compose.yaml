version: '3.8'

services:
  agentlabs:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agentlabs
    environment:
      - AGENT_NAME=AgentLabs
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
