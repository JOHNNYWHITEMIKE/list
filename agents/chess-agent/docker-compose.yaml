version: '3.8'

services:
  chess-agent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: chess-agent
    environment:
      - AGENT_NAME=Chess-Agent
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
