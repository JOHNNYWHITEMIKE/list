version: '3.8'

services:
  xagent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: xagent
    environment:
      - AGENT_NAME=XAgent
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
