version: '3.8'

services:
  nemo-guardrails:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nemo-guardrails
    environment:
      - AGENT_NAME=NeMo Guardrails
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
