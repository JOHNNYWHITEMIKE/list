version: '3.8'

services:
  latitude-llm:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: latitude-llm
    environment:
      - AGENT_NAME=Latitude-Llm
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
