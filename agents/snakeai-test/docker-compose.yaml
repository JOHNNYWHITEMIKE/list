version: '3.8'

services:
  snakeai-test:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: snakeai-test
    environment:
      - AGENT_NAME=Snakeai-Test
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
