version: '3.8'

services:
  wxflows:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: wxflows
    environment:
      - AGENT_NAME=Wxflows
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
