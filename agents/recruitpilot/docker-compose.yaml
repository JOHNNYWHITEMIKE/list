version: '3.8'

services:
  recruitpilot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: recruitpilot
    environment:
      - AGENT_NAME=Recruitpilot
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
