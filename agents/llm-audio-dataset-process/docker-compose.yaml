version: '3.8'

services:
  llm-audio-dataset-process:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llm-audio-dataset-process
    environment:
      - AGENT_NAME=Llm-Audio-Dataset-Process
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
