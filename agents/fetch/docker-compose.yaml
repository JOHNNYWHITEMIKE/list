version: '3.8'

services:
  fetch:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fetch
    environment:
      - AGENT_NAME=Fetch
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
