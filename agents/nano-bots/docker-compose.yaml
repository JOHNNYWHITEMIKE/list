version: '3.8'

services:
  nano-bots:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nano-bots
    environment:
      - AGENT_NAME=Nano-Bots
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
