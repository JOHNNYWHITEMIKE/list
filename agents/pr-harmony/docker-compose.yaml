version: '3.8'

services:
  pr-harmony:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pr-harmony
    environment:
      - AGENT_NAME=Pr-Harmony
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
