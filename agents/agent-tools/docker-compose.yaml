version: '3.8'

services:
  agent-tools:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agent-tools
    environment:
      - AGENT_NAME=Agent Tools
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
