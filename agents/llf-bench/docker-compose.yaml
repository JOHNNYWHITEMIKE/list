version: '3.8'

services:
  llf-bench:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llf-bench
    environment:
      - AGENT_NAME=Llf-Bench
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
