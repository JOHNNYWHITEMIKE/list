version: '3.8'

services:
  ai-agents:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-agents
    environment:
      - AGENT_NAME=Ai-Agents
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
