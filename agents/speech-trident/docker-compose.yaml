version: '3.8'

services:
  speech-trident:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: speech-trident
    environment:
      - AGENT_NAME=Speech-Trident
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
