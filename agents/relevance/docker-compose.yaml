version: '3.8'

services:
  relevance:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: relevance
    environment:
      - AGENT_NAME=Relevance
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
