version: '3.8'

services:
  bespoke_automata:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bespoke_automata
    environment:
      - AGENT_NAME=Bespoke_Automata
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
