version: '3.8'

services:
  agents-by-ai-waves:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agents-by-ai-waves
    environment:
      - AGENT_NAME=Agents by AI Waves
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
