version: '3.8'

services:
  chatdev:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: chatdev
    environment:
      - AGENT_NAME=ChatDev
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
