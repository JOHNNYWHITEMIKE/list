version: '3.8'

services:
  renellm:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: renellm
    environment:
      - AGENT_NAME=Renellm
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
