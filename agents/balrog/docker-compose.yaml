version: '3.8'

services:
  balrog:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: balrog
    environment:
      - AGENT_NAME=Balrog
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
