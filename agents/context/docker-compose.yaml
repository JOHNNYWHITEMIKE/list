version: '3.8'

services:
  context:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: context
    environment:
      - AGENT_NAME=Context
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
