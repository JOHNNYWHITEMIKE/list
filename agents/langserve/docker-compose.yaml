version: '3.8'

services:
  langserve:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: langserve
    environment:
      - AGENT_NAME=LangServe
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
