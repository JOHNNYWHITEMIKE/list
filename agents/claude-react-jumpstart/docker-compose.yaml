version: '3.8'

services:
  claude-react-jumpstart:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: claude-react-jumpstart
    environment:
      - AGENT_NAME=Claude-React-Jumpstart
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
