version: '3.8'

services:
  llm_bxn_dataset:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llm_bxn_dataset
    environment:
      - AGENT_NAME=Llm_Bxn_Dataset
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
