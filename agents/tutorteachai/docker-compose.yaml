version: '3.8'

services:
  tutorteachai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tutorteachai
    environment:
      - AGENT_NAME=Tutorteach.Ai
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
