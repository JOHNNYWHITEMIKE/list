version: '3.8'

services:
  llm-bedrock:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llm-bedrock
    environment:
      - AGENT_NAME=Llm-Bedrock
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
