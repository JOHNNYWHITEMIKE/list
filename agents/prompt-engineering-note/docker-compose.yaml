version: '3.8'

services:
  prompt-engineering-note:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: prompt-engineering-note
    environment:
      - AGENT_NAME=Prompt-Engineering-Note
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
