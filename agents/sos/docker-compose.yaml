version: '3.8'

services:
  sos:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sos
    environment:
      - AGENT_NAME=Sos
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
