version: '3.8'

services:
  aws-ddk:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aws-ddk
    environment:
      - AGENT_NAME=Aws-Ddk
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
