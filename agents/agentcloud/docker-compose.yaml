version: '3.8'

services:
  agentcloud:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agentcloud
    environment:
      - AGENT_NAME=Agentcloud
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
