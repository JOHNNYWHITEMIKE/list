version: '3.8'

services:
  harbor:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: harbor
    environment:
      - AGENT_NAME=Harbor
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
