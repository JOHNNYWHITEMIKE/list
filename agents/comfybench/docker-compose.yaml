version: '3.8'

services:
  comfybench:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: comfybench
    environment:
      - AGENT_NAME=Comfybench
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
