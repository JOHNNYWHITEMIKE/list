version: '3.8'

services:
  onchain-ai-starter:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: onchain-ai-starter
    environment:
      - AGENT_NAME=Onchain-Ai-Starter
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
