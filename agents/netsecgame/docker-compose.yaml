version: '3.8'

services:
  netsecgame:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: netsecgame
    environment:
      - AGENT_NAME=Netsecgame
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
