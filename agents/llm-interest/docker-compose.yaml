version: '3.8'

services:
  llm-interest:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llm-interest
    environment:
      - AGENT_NAME=Llm-Interest
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
