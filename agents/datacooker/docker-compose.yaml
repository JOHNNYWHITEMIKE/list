version: '3.8'

services:
  datacooker:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: datacooker
    environment:
      - AGENT_NAME=Datacooker
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
