version: '3.8'

services:
  ai-code-gen:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-code-gen
    environment:
      - AGENT_NAME=Ai-Code-Gen
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
