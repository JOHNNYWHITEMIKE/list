version: '3.8'

services:
  smartcall-ai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: smartcall-ai
    environment:
      - AGENT_NAME=Smartcall-Ai
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
