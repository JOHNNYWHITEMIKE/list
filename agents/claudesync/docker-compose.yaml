version: '3.8'

services:
  claudesync:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: claudesync
    environment:
      - AGENT_NAME=Claudesync
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
