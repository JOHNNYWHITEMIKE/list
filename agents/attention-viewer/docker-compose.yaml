version: '3.8'

services:
  attention-viewer:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: attention-viewer
    environment:
      - AGENT_NAME=Attention-Viewer
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
