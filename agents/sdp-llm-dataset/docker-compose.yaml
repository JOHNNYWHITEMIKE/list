version: '3.8'

services:
  sdp-llm-dataset:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sdp-llm-dataset
    environment:
      - AGENT_NAME=Sdp-Llm-Dataset
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
