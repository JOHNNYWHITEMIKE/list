version: '3.8'

services:
  mg-3-aiethics:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mg-3-aiethics
    environment:
      - AGENT_NAME=Mg-3-Aiethics
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
