version: '3.8'

services:
  ethicscore:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ethicscore
    environment:
      - AGENT_NAME=Ethicscore
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
