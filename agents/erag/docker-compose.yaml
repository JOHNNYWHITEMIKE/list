version: '3.8'

services:
  erag:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: erag
    environment:
      - AGENT_NAME=Erag
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
