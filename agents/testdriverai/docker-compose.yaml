version: '3.8'

services:
  testdriverai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: testdriverai
    environment:
      - AGENT_NAME=Testdriverai
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
