version: '3.8'

services:
  autogpt-next-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: autogpt-next-web
    environment:
      - AGENT_NAME=Autogpt-Next-Web
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
