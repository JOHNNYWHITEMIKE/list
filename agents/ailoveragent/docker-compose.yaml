version: '3.8'

services:
  ailoveragent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ailoveragent
    environment:
      - AGENT_NAME=Ailoveragent
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
