version: '3.8'

services:
  ella:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ella
    environment:
      - AGENT_NAME=Ella
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
