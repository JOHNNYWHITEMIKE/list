version: '3.8'

services:
  syncode:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: syncode
    environment:
      - AGENT_NAME=Syncode
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
