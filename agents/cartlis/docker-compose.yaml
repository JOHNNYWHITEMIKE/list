version: '3.8'

services:
  cartlis:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cartlis
    environment:
      - AGENT_NAME=Cartlis
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
