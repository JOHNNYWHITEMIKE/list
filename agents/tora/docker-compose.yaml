version: '3.8'

services:
  tora:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tora
    environment:
      - AGENT_NAME=Tora
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
