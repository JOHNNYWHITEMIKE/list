version: '3.8'

services:
  ert-runnerel:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ert-runnerel
    environment:
      - AGENT_NAME=Ert-Runner.El
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
