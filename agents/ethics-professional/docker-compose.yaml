version: '3.8'

services:
  ethics-professional:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ethics-professional
    environment:
      - AGENT_NAME=Ethics-Professional
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
