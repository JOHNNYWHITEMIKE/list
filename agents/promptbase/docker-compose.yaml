version: '3.8'

services:
  promptbase:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: promptbase
    environment:
      - AGENT_NAME=Promptbase
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
