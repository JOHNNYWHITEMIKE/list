version: '3.8'

services:
  instantneo:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: instantneo
    environment:
      - AGENT_NAME=Instantneo
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
