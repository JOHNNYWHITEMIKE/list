version: '3.8'

services:
  design_diagrams:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: design_diagrams
    environment:
      - AGENT_NAME=Design_Diagrams
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
