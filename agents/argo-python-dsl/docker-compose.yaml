version: '3.8'

services:
  argo-python-dsl:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: argo-python-dsl
    environment:
      - AGENT_NAME=Argo-Python-Dsl
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
