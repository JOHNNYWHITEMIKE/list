version: '3.8'

services:
  synthflow-ai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: synthflow-ai
    environment:
      - AGENT_NAME=Synthflow AI
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
