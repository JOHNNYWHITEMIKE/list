version: '3.8'

services:
  texera:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: texera
    environment:
      - AGENT_NAME=Texera
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
