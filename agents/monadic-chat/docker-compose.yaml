version: '3.8'

services:
  monadic-chat:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: monadic-chat
    environment:
      - AGENT_NAME=Monadic-Chat
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
