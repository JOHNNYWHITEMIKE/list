version: '3.8'

services:
  anygpt:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: anygpt
    environment:
      - AGENT_NAME=Anygpt
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
