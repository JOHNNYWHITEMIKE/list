version: '3.8'

services:
  alfy:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: alfy
    environment:
      - AGENT_NAME=Alfy
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
