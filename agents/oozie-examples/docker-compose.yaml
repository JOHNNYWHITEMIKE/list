version: '3.8'

services:
  oozie-examples:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: oozie-examples
    environment:
      - AGENT_NAME=Oozie-Examples
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
