version: '3.8'

services:
  tortoise-tts:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tortoise-tts
    environment:
      - AGENT_NAME=tortoise-tts
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
