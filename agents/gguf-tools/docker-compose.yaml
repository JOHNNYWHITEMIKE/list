version: '3.8'

services:
  gguf-tools:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: gguf-tools
    environment:
      - AGENT_NAME=Gguf-Tools
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
