version: '3.8'

services:
  lex-fridman-dataset-llm:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: lex-fridman-dataset-llm
    environment:
      - AGENT_NAME=Lex-Fridman-Dataset-Llm
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
