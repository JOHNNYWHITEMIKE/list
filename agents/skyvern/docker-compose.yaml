version: '3.8'

services:
  skyvern:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: skyvern
    environment:
      - AGENT_NAME=Skyvern
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
