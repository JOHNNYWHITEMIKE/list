version: '3.8'

services:
  aios:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aios
    environment:
      - AGENT_NAME=AIOS
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
