version: '3.8'

services:
  blueprints:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: blueprints
    environment:
      - AGENT_NAME=Blueprints
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
