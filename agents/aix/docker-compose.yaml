version: '3.8'

services:
  aix:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aix
    environment:
      - AGENT_NAME=Aix
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
