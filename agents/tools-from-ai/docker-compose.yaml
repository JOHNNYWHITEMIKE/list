version: '3.8'

services:
  tools-from-ai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tools-from-ai
    environment:
      - AGENT_NAME=Tools-From-Ai
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
