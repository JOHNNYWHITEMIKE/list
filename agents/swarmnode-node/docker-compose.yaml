version: '3.8'

services:
  swarmnode-node:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: swarmnode-node
    environment:
      - AGENT_NAME=Swarmnode-Node
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
