version: '3.8'

services:
  git-smart:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: git-smart
    environment:
      - AGENT_NAME=Git-Smart
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
