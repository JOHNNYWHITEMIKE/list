version: '3.8'

services:
  crewai-test:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: crewai-test
    environment:
      - AGENT_NAME=Crewai-Test
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
