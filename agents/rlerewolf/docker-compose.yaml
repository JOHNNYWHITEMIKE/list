version: '3.8'

services:
  rlerewolf:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rlerewolf
    environment:
      - AGENT_NAME=Rlerewolf
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
