version: '3.8'

services:
  pydantic:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pydantic
    environment:
      - AGENT_NAME=Pydantic
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
