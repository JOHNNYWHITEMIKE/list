version: '3.8'

services:
  arxivrag:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: arxivrag
    environment:
      - AGENT_NAME=Arxivrag
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
