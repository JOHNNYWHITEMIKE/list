version: '3.8'

services:
  tactic:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tactic
    environment:
      - AGENT_NAME=Tactic
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
