version: '3.8'

services:
  codel:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: codel
    environment:
      - AGENT_NAME=Codel
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
