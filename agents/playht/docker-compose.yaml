version: '3.8'

services:
  playht:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: playht
    environment:
      - AGENT_NAME=PlayHT
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
