version: '3.8'

services:
  llms-learning:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llms-learning
    environment:
      - AGENT_NAME=Llms-Learning
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
