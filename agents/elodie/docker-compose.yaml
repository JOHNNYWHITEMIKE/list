version: '3.8'

services:
  elodie:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: elodie
    environment:
      - AGENT_NAME=Elodie
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
