version: '3.8'

services:
  guardrails:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: guardrails
    environment:
      - AGENT_NAME=Guardrails
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
