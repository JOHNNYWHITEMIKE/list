version: '3.8'

services:
  llm-benchmarks:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llm-benchmarks
    environment:
      - AGENT_NAME=Llm-Benchmarks
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
