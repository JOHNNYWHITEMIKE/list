version: '3.8'

services:
  fireai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fireai
    environment:
      - AGENT_NAME=Fireai
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
