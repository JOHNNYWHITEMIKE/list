version: '3.8'

services:
  promptagent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: promptagent
    environment:
      - AGENT_NAME=Promptagent
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
