version: '3.8'

services:
  unchained:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: unchained
    environment:
      - AGENT_NAME=Unchained
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
