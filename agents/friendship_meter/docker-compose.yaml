version: '3.8'

services:
  friendship_meter:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: friendship_meter
    environment:
      - AGENT_NAME=Friendship_Meter
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
