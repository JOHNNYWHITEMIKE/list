version: '3.8'

services:
  libmolgrid:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: libmolgrid
    environment:
      - AGENT_NAME=Libmolgrid
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
