version: '3.8'

services:
  promptpet:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: promptpet
    environment:
      - AGENT_NAME=Promptpet
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
