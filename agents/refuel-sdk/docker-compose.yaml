version: '3.8'

services:
  refuel-sdk:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: refuel-sdk
    environment:
      - AGENT_NAME=Refuel-Sdk
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
