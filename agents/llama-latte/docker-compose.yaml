version: '3.8'

services:
  llama-latte:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llama-latte
    environment:
      - AGENT_NAME=Llama-Latte
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
