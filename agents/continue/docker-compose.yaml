version: '3.8'

services:
  continue:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: continue
    environment:
      - AGENT_NAME=Continue
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
