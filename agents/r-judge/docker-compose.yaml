version: '3.8'

services:
  r-judge:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: r-judge
    environment:
      - AGENT_NAME=R-Judge
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
