version: '3.8'

services:
  mobilellm:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mobilellm
    environment:
      - AGENT_NAME=Mobilellm
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
