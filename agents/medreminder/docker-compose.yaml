version: '3.8'

services:
  medreminder:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: medreminder
    environment:
      - AGENT_NAME=Medreminder
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
