version: '3.8'

services:
  superagi:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: superagi
    environment:
      - AGENT_NAME=SuperAGI
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
