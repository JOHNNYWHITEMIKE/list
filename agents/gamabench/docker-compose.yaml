version: '3.8'

services:
  gamabench:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: gamabench
    environment:
      - AGENT_NAME=Gamabench
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
