version: '3.8'

services:
  dbt-llm-tools:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dbt-llm-tools
    environment:
      - AGENT_NAME=Dbt-Llm-Tools
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
