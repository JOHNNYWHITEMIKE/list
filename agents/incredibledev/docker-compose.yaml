version: '3.8'

services:
  incredibledev:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: incredibledev
    environment:
      - AGENT_NAME=Incredible.Dev
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
