version: '3.8'

services:
  awesome-llm-long-context-modeling:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: awesome-llm-long-context-modeling
    environment:
      - AGENT_NAME=Awesome-Llm-Long-Context-Modeling
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
