version: '3.8'

services:
  intellichunk:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: intellichunk
    environment:
      - AGENT_NAME=Intellichunk
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
