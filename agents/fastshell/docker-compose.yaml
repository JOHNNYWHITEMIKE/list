version: '3.8'

services:
  fastshell:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fastshell
    environment:
      - AGENT_NAME=Fastshell
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
