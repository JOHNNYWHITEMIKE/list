version: '3.8'

services:
  opengptandbeyond:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: opengptandbeyond
    environment:
      - AGENT_NAME=Opengptandbeyond
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
