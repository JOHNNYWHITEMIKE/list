version: '3.8'

services:
  extractthinker:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: extractthinker
    environment:
      - AGENT_NAME=Extractthinker
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
