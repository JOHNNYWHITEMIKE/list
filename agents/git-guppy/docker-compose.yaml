version: '3.8'

services:
  git-guppy:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: git-guppy
    environment:
      - AGENT_NAME=Git-Guppy
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
