version: '3.8'

services:
  dravid:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dravid
    environment:
      - AGENT_NAME=Dravid
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
