version: '3.8'

services:
  ephemeris:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ephemeris
    environment:
      - AGENT_NAME=Ephemeris
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
