version: '3.8'

services:
  deepunit:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: deepunit
    environment:
      - AGENT_NAME=Deepunit
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
