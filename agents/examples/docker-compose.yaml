version: '3.8'

services:
  examples:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: examples
    environment:
      - AGENT_NAME=Examples
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
