version: '3.8'

services:
  finetuning_llms:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: finetuning_llms
    environment:
      - AGENT_NAME=Finetuning_Llms
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
