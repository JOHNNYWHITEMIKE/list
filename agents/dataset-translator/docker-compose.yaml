version: '3.8'

services:
  dataset-translator:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dataset-translator
    environment:
      - AGENT_NAME=Dataset-Translator
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
