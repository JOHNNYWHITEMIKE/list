version: '3.8'

services:
  autogpt:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: autogpt
    environment:
      - AGENT_NAME=AutoGPT
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
