version: '3.8'

services:
  canary:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: canary
    environment:
      - AGENT_NAME=Canary
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
