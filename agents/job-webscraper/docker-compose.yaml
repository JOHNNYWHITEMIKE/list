version: '3.8'

services:
  job-webscraper:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: job-webscraper
    environment:
      - AGENT_NAME=Job-Webscraper
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
