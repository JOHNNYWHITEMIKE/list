version: '3.8'

services:
  promptify:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: promptify
    environment:
      - AGENT_NAME=Promptify
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
