version: '3.8'

services:
  explorer:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: explorer
    environment:
      - AGENT_NAME=Explorer
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
