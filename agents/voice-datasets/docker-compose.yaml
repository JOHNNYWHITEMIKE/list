version: '3.8'

services:
  voice-datasets:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: voice-datasets
    environment:
      - AGENT_NAME=Voice datasets
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
