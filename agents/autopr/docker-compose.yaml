version: '3.8'

services:
  autopr:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: autopr
    environment:
      - AGENT_NAME=Autopr
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
