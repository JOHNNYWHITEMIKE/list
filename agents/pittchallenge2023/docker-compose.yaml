version: '3.8'

services:
  pittchallenge2023:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pittchallenge2023
    environment:
      - AGENT_NAME=Pittchallenge2023
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
