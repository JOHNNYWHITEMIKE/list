version: '3.8'

services:
  llm_optimize:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llm_optimize
    environment:
      - AGENT_NAME=Llm_Optimize
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
