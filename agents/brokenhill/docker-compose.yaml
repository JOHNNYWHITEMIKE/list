version: '3.8'

services:
  brokenhill:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: brokenhill
    environment:
      - AGENT_NAME=Brokenhill
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
