version: '3.8'

services:
  ambrose:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ambrose
    environment:
      - AGENT_NAME=Ambrose
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
