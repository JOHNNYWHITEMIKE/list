version: '3.8'

services:
  langgraph:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: langgraph
    environment:
      - AGENT_NAME=LangGraph
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
