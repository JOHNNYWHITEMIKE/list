version: '3.8'

services:
  flux:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: flux
    environment:
      - AGENT_NAME=Flux
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
