version: '3.8'

services:
  vonage:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vonage
    environment:
      - AGENT_NAME=Vonage
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
