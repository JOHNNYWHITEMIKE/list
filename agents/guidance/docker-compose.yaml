version: '3.8'

services:
  guidance:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: guidance
    environment:
      - AGENT_NAME=Guidance
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
