version: '3.8'

services:
  advanced-gpts:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: advanced-gpts
    environment:
      - AGENT_NAME=Advanced-Gpts
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
