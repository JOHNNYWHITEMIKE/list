version: '3.8'

services:
  mql:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mql
    environment:
      - AGENT_NAME=Mql
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
