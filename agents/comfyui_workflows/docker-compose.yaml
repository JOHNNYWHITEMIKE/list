version: '3.8'

services:
  comfyui_workflows:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: comfyui_workflows
    environment:
      - AGENT_NAME=Comfyui_Workflows
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
