version: '3.8'

services:
  testai-agent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: testai-agent
    environment:
      - AGENT_NAME=Testai-Agent
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
