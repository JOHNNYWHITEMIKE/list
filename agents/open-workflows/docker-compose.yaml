version: '3.8'

services:
  open-workflows:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: open-workflows
    environment:
      - AGENT_NAME=Open-Workflows
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
