version: '3.8'

services:
  ivycheck:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ivycheck
    environment:
      - AGENT_NAME=IvyCheck
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
