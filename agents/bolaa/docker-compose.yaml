version: '3.8'

services:
  bolaa:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bolaa
    environment:
      - AGENT_NAME=Bolaa
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
