version: '3.8'

services:
  conversation-data:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: conversation-data
    environment:
      - AGENT_NAME=Conversation-Data
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
