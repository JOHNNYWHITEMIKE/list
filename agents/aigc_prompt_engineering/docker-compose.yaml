version: '3.8'

services:
  aigc_prompt_engineering:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aigc_prompt_engineering
    environment:
      - AGENT_NAME=Aigc_Prompt_Engineering
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
