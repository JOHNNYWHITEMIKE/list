version: '3.8'

services:
  lumos:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: lumos
    environment:
      - AGENT_NAME=Lumos
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
