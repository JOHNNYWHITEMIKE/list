version: '3.8'

services:
  awesome-azure-openai-llm:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: awesome-azure-openai-llm
    environment:
      - AGENT_NAME=Awesome-Azure-Openai-Llm
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
