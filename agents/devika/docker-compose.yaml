version: '3.8'

services:
  devika:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: devika
    environment:
      - AGENT_NAME=Devika
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
