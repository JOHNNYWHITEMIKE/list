version: '3.8'

services:
  petercat:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: petercat
    environment:
      - AGENT_NAME=Petercat
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
