version: '3.8'

services:
  awesome-llm-human-preference-datasets:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: awesome-llm-human-preference-datasets
    environment:
      - AGENT_NAME=Awesome-Llm-Human-Preference-Datasets
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
