version: '3.8'

services:
  heb-gen-ai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: heb-gen-ai
    environment:
      - AGENT_NAME=Heb-Gen-Ai
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
