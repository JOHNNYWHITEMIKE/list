version: '3.8'

services:
  pyspur:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pyspur
    environment:
      - AGENT_NAME=Pyspur
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
