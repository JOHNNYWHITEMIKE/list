version: '3.8'

services:
  pieces:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pieces
    environment:
      - AGENT_NAME=Pieces
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
