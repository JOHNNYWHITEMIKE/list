version: '3.8'

services:
  github-script:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: github-script
    environment:
      - AGENT_NAME=Github-Script
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
