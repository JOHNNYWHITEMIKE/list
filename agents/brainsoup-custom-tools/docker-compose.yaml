version: '3.8'

services:
  brainsoup-custom-tools:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: brainsoup-custom-tools
    environment:
      - AGENT_NAME=BrainSoup Custom Tools
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
