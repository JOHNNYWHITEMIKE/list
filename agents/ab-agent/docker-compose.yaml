version: '3.8'

services:
  ab-agent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ab-agent
    environment:
      - AGENT_NAME=Ab-Agent
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
