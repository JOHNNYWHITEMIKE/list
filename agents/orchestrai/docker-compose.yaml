version: '3.8'

services:
  orchestrai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: orchestrai
    environment:
      - AGENT_NAME=Orchestrai
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
