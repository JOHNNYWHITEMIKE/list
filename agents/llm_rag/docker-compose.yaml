version: '3.8'

services:
  llm_rag:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llm_rag
    environment:
      - AGENT_NAME=Llm_Rag
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
