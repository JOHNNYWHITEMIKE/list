version: '3.8'

services:
  flowbench:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: flowbench
    environment:
      - AGENT_NAME=Flowbench
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
