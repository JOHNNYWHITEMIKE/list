version: '3.8'

services:
  gulp:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: gulp
    environment:
      - AGENT_NAME=Gulp
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
