version: '3.8'

services:
  nipype:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nipype
    environment:
      - AGENT_NAME=Nipype
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
