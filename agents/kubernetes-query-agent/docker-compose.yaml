version: '3.8'

services:
  kubernetes-query-agent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: kubernetes-query-agent
    environment:
      - AGENT_NAME=Kubernetes-Query-Agent
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
