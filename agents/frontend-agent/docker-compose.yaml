version: '3.8'

services:
  frontend-agent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: frontend-agent
    environment:
      - AGENT_NAME=Frontend-Agent
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
