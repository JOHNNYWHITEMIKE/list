version: '3.8'

services:
  pathology_llm:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pathology_llm
    environment:
      - AGENT_NAME=Pathology_Llm
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
