version: '3.8'

services:
  rgs:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rgs
    environment:
      - AGENT_NAME=Rgs
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
