version: '3.8'

services:
  airline-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: airline-api
    environment:
      - AGENT_NAME=Airline-Api
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
