version: '3.8'

services:
  llm-tools:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llm-tools
    environment:
      - AGENT_NAME=Llm-Tools
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
