version: '3.8'

services:
  docgenie:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: docgenie
    environment:
      - AGENT_NAME=Docgenie
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
