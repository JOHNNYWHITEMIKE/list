version: '3.8'

services:
  ai-ethics-panel:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-ethics-panel
    environment:
      - AGENT_NAME=Ai-Ethics-Panel
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
