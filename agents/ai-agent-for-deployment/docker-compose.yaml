version: '3.8'

services:
  ai-agent-for-deployment:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-agent-for-deployment
    environment:
      - AGENT_NAME=Ai-Agent-For-Deployment
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
