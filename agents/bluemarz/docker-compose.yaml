version: '3.8'

services:
  bluemarz:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bluemarz
    environment:
      - AGENT_NAME=Bluemarz
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
