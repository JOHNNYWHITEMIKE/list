version: '3.8'

services:
  de-bench:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: de-bench
    environment:
      - AGENT_NAME=De-Bench
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
