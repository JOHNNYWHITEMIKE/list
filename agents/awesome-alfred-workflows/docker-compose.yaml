version: '3.8'

services:
  awesome-alfred-workflows:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: awesome-alfred-workflows
    environment:
      - AGENT_NAME=Awesome-Alfred-Workflows
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
