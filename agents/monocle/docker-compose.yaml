version: '3.8'

services:
  monocle:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: monocle
    environment:
      - AGENT_NAME=Monocle
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
