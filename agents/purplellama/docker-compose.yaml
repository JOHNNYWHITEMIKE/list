version: '3.8'

services:
  purplellama:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: purplellama
    environment:
      - AGENT_NAME=Purplellama
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
