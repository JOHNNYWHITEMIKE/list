version: '3.8'

services:
  llm-adapters:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llm-adapters
    environment:
      - AGENT_NAME=Llm-Adapters
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
