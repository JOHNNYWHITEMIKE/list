version: '3.8'

services:
  sentiment-rating:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sentiment-rating
    environment:
      - AGENT_NAME=Sentiment-Rating
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
