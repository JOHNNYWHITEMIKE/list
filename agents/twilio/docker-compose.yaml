version: '3.8'

services:
  twilio:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: twilio
    environment:
      - AGENT_NAME=Twilio
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
