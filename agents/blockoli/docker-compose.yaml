version: '3.8'

services:
  blockoli:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: blockoli
    environment:
      - AGENT_NAME=Blockoli
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
