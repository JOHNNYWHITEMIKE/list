version: '3.8'

services:
  baseai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: baseai
    environment:
      - AGENT_NAME=Baseai
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
