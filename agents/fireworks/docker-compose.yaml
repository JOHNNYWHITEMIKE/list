version: '3.8'

services:
  fireworks:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fireworks
    environment:
      - AGENT_NAME=Fireworks
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
