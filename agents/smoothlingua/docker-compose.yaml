version: '3.8'

services:
  smoothlingua:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: smoothlingua
    environment:
      - AGENT_NAME=Smoothlingua
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
