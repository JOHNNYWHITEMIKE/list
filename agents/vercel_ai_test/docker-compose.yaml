version: '3.8'

services:
  vercel_ai_test:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vercel_ai_test
    environment:
      - AGENT_NAME=Vercel_Ai_Test
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
