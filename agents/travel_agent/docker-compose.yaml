version: '3.8'

services:
  travel_agent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: travel_agent
    environment:
      - AGENT_NAME=Travel_Agent
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
