version: '3.8'

services:
  qlora-llm:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: qlora-llm
    environment:
      - AGENT_NAME=Qlora-Llm
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
