version: '3.8'

services:
  diffsensei:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: diffsensei
    environment:
      - AGENT_NAME=Diffsensei
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
