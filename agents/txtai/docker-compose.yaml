version: '3.8'

services:
  txtai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: txtai
    environment:
      - AGENT_NAME=Txtai
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
