version: '3.8'

services:
  sammo:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sammo
    environment:
      - AGENT_NAME=Sammo
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
