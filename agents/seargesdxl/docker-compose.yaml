version: '3.8'

services:
  seargesdxl:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: seargesdxl
    environment:
      - AGENT_NAME=Seargesdxl
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
