version: '3.8'

services:
  textbook_quality:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: textbook_quality
    environment:
      - AGENT_NAME=Textbook_Quality
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
