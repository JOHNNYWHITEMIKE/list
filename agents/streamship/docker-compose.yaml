version: '3.8'

services:
  streamship:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: streamship
    environment:
      - AGENT_NAME=Streamship
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
