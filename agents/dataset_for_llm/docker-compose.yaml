version: '3.8'

services:
  dataset_for_llm:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dataset_for_llm
    environment:
      - AGENT_NAME=Dataset_For_Llm
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
