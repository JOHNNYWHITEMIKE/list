version: '3.8'

services:
  prompt-engineering-tutior:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: prompt-engineering-tutior
    environment:
      - AGENT_NAME=Prompt-Engineering-Tutior
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
