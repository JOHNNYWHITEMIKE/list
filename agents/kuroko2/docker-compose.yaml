version: '3.8'

services:
  kuroko2:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: kuroko2
    environment:
      - AGENT_NAME=Kuroko2
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
