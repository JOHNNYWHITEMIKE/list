version: '3.8'

services:
  n8n-workflows:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: n8n-workflows
    environment:
      - AGENT_NAME=N8N-Workflows
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
