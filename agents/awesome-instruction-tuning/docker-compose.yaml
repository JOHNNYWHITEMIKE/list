version: '3.8'

services:
  awesome-instruction-tuning:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: awesome-instruction-tuning
    environment:
      - AGENT_NAME=Awesome-Instruction-Tuning
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
