version: '3.8'

services:
  debatellm:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: debatellm
    environment:
      - AGENT_NAME=Debatellm
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
