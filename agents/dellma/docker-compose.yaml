version: '3.8'

services:
  dellma:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dellma
    environment:
      - AGENT_NAME=Dellma
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
