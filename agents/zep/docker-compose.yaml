version: '3.8'

services:
  zep:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: zep
    environment:
      - AGENT_NAME=Zep
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
