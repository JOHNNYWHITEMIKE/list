version: '3.8'

services:
  hume-ai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: hume-ai
    environment:
      - AGENT_NAME=Hume AI
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
