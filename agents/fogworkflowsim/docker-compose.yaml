version: '3.8'

services:
  fogworkflowsim:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fogworkflowsim
    environment:
      - AGENT_NAME=Fogworkflowsim
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
