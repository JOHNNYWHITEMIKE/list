version: '3.8'

services:
  ai-agent-crew:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-agent-crew
    environment:
      - AGENT_NAME=AI Agent Crew
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
