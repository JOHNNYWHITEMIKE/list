version: '3.8'

services:
  agentic-ai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agentic-ai
    environment:
      - AGENT_NAME=Agentic-Ai-
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
