version: '3.8'

services:
  libertai-agents:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: libertai-agents
    environment:
      - AGENT_NAME=Libertai-Agents
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
