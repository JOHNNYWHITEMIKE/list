version: '3.8'

services:
  adfa2:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: adfa2
    environment:
      - AGENT_NAME=Adfa2
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
