version: '3.8'

services:
  langchain-js:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: langchain-js
    environment:
      - AGENT_NAME=LangChain JS
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
