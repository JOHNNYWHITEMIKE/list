version: '3.8'

services:
  mergekit:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mergekit
    environment:
      - AGENT_NAME=Mergekit
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
