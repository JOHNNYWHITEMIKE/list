version: '3.8'

services:
  viewflow:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: viewflow
    environment:
      - AGENT_NAME=Viewflow
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
