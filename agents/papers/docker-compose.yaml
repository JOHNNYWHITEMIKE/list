version: '3.8'

services:
  papers:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: papers
    environment:
      - AGENT_NAME=Papers
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
