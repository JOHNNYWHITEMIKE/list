version: '3.8'

services:
  ai-in-medicine:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-in-medicine
    environment:
      - AGENT_NAME=Ai-In-Medicine
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
