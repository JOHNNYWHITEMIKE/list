version: '3.8'

services:
  story-prompt:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: story-prompt
    environment:
      - AGENT_NAME=Story-Prompt
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
