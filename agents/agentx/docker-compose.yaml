version: '3.8'

services:
  agentx:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agentx
    environment:
      - AGENT_NAME=Agentx
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
