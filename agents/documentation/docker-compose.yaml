version: '3.8'

services:
  documentation:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: documentation
    environment:
      - AGENT_NAME=Documentation
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
