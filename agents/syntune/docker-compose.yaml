version: '3.8'

services:
  syntune:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: syntune
    environment:
      - AGENT_NAME=Syntune
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
