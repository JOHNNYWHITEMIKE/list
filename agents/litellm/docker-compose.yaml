version: '3.8'

services:
  litellm:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: litellm
    environment:
      - AGENT_NAME=LiteLLM
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
