version: '3.8'

services:
  neural-compressor:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: neural-compressor
    environment:
      - AGENT_NAME=Neural-Compressor
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
