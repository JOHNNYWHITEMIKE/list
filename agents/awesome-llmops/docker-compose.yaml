version: '3.8'

services:
  awesome-llmops:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: awesome-llmops
    environment:
      - AGENT_NAME=Awesome-Llmops
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
