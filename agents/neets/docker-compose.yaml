version: '3.8'

services:
  neets:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: neets
    environment:
      - AGENT_NAME=Neets
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
