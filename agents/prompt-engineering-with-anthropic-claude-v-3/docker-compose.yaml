version: '3.8'

services:
  prompt-engineering-with-anthropic-claude-v-3:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: prompt-engineering-with-anthropic-claude-v-3
    environment:
      - AGENT_NAME=Prompt-Engineering-With-Anthropic-Claude-V-3
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
