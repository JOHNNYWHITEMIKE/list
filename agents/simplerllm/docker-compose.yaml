version: '3.8'

services:
  simplerllm:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: simplerllm
    environment:
      - AGENT_NAME=Simplerllm
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
