version: '3.8'

services:
  tinyllm:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tinyllm
    environment:
      - AGENT_NAME=Tinyllm
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
