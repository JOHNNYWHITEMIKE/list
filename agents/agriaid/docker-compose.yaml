version: '3.8'

services:
  agriaid:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agriaid
    environment:
      - AGENT_NAME=Agriaid
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
