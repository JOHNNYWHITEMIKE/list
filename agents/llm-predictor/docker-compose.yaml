version: '3.8'

services:
  llm-predictor:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llm-predictor
    environment:
      - AGENT_NAME=Llm-Predictor
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
