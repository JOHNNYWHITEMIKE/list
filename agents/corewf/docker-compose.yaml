version: '3.8'

services:
  corewf:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: corewf
    environment:
      - AGENT_NAME=Corewf
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
