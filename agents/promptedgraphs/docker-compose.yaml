version: '3.8'

services:
  promptedgraphs:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: promptedgraphs
    environment:
      - AGENT_NAME=Promptedgraphs
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
