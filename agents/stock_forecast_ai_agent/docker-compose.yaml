version: '3.8'

services:
  stock_forecast_ai_agent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: stock_forecast_ai_agent
    environment:
      - AGENT_NAME=Stock_Forecast_Ai_Agent
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
