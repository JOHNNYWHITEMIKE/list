version: '3.8'

services:
  instructor:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: instructor
    environment:
      - AGENT_NAME=Instructor
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
