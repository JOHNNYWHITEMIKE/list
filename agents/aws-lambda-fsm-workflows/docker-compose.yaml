version: '3.8'

services:
  aws-lambda-fsm-workflows:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aws-lambda-fsm-workflows
    environment:
      - AGENT_NAME=Aws-Lambda-Fsm-Workflows
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
