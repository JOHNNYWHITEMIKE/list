version: '3.8'

services:
  ai-pacman-classification:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-pacman-classification
    environment:
      - AGENT_NAME=Ai-Pacman-Classification
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
