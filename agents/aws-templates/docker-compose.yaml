version: '3.8'

services:
  aws-templates:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aws-templates
    environment:
      - AGENT_NAME=Aws-Templates
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
