version: '3.8'

services:
  maestro:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: maestro
    environment:
      - AGENT_NAME=Maestro
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
