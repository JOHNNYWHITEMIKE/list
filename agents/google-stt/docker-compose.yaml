version: '3.8'

services:
  google-stt:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: google-stt
    environment:
      - AGENT_NAME=Google STT
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
