version: '3.8'

services:
  llm_tools:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llm_tools
    environment:
      - AGENT_NAME=Llm_Tools
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
