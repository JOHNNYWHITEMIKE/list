version: '3.8'

services:
  gpt-engineer:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: gpt-engineer
    environment:
      - AGENT_NAME=GPT Engineer
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
