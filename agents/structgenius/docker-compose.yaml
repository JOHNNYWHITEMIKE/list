version: '3.8'

services:
  structgenius:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: structgenius
    environment:
      - AGENT_NAME=Structgenius
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
