version: '3.8'

services:
  with:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: with
    environment:
      - AGENT_NAME=With
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
