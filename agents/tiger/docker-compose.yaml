version: '3.8'

services:
  tiger:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tiger
    environment:
      - AGENT_NAME=Tiger
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
