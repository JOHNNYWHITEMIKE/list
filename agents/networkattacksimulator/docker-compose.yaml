version: '3.8'

services:
  networkattacksimulator:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: networkattacksimulator
    environment:
      - AGENT_NAME=Networkattacksimulator
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
