version: '3.8'

services:
  dingo:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dingo
    environment:
      - AGENT_NAME=Dingo
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
