version: '3.8'

services:
  konfig:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: konfig
    environment:
      - AGENT_NAME=Konfig
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
