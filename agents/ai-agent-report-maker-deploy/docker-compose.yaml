version: '3.8'

services:
  ai-agent-report-maker-deploy:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-agent-report-maker-deploy
    environment:
      - AGENT_NAME=Ai-Agent-Report-Maker-Deploy
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
