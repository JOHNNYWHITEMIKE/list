version: '3.8'

services:
  perplexity:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: perplexity
    environment:
      - AGENT_NAME=Perplexity
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
