version: '3.8'

services:
  yeoman:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: yeoman
    environment:
      - AGENT_NAME=Yeoman
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
