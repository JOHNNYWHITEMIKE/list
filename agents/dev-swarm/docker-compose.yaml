version: '3.8'

services:
  dev-swarm:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dev-swarm
    environment:
      - AGENT_NAME=Dev-Swarm
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
