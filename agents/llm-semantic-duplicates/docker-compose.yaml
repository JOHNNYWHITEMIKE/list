version: '3.8'

services:
  llm-semantic-duplicates:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llm-semantic-duplicates
    environment:
      - AGENT_NAME=Llm-Semantic-Duplicates
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
