version: '3.8'

services:
  deepinfra:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: deepinfra
    environment:
      - AGENT_NAME=DeepInfra
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
