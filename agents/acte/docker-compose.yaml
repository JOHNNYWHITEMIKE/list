version: '3.8'

services:
  acte:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: acte
    environment:
      - AGENT_NAME=Acte
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
