version: '3.8'

services:
  sphnx:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sphnx
    environment:
      - AGENT_NAME=Sphnx
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
