version: '3.8'

services:
  alfredworkflowcom:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: alfredworkflowcom
    environment:
      - AGENT_NAME=Alfredworkflow.Com
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
