version: '3.8'

services:
  promptimize:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: promptimize
    environment:
      - AGENT_NAME=Promptimize
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
