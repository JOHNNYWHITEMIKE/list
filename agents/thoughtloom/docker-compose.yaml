version: '3.8'

services:
  thoughtloom:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: thoughtloom
    environment:
      - AGENT_NAME=Thoughtloom
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
