version: '3.8'

services:
  trafilatura:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: trafilatura
    environment:
      - AGENT_NAME=Trafilatura
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
