version: '3.8'

services:
  learning-prompt:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: learning-prompt
    environment:
      - AGENT_NAME=Learning-Prompt
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
