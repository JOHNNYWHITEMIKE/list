version: '3.8'

services:
  book-dataset:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: book-dataset
    environment:
      - AGENT_NAME=Book-Dataset
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
