version: '3.8'

services:
  dataset-generator-for-llm-finetuning:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dataset-generator-for-llm-finetuning
    environment:
      - AGENT_NAME=Dataset-Generator-For-Llm-Finetuning
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
