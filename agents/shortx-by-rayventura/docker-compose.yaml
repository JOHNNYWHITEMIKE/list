version: '3.8'

services:
  shortx-by-rayventura:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: shortx-by-rayventura
    environment:
      - AGENT_NAME=ShortX by RayVentura
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
