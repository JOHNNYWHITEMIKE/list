version: '3.8'

services:
  databerry:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: databerry
    environment:
      - AGENT_NAME=databerry
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
