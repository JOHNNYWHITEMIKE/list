version: '3.8'

services:
  agentops:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agentops
    environment:
      - AGENT_NAME=Agentops
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
