version: '3.8'

services:
  browsingagent-by-agency-swarm:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: browsingagent-by-agency-swarm
    environment:
      - AGENT_NAME=BrowsingAgent by Agency Swarm
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
