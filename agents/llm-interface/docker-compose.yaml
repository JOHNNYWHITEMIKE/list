version: '3.8'

services:
  llm-interface:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llm-interface
    environment:
      - AGENT_NAME=Llm-Interface
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
