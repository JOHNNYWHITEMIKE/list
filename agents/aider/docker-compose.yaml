version: '3.8'

services:
  aider:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aider
    environment:
      - AGENT_NAME=Aider
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
