version: '3.8'

services:
  ruote-kit:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ruote-kit
    environment:
      - AGENT_NAME=Ruote-Kit
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
