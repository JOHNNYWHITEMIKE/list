version: '3.8'

services:
  awesome-finllms:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: awesome-finllms
    environment:
      - AGENT_NAME=Awesome-Finllms
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
