version: '3.8'

services:
  glm-edge:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: glm-edge
    environment:
      - AGENT_NAME=Glm-Edge
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
