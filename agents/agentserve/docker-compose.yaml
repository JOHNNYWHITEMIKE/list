version: '3.8'

services:
  agentserve:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agentserve
    environment:
      - AGENT_NAME=Agentserve
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
