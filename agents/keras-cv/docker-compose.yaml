version: '3.8'

services:
  keras-cv:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: keras-cv
    environment:
      - AGENT_NAME=Keras-Cv
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
