version: '3.8'

services:
  datadog:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: datadog
    environment:
      - AGENT_NAME=Datadog
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
