version: '3.8'

services:
  fastmcp:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fastmcp
    environment:
      - AGENT_NAME=Fastmcp
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
