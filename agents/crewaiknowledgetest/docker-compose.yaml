version: '3.8'

services:
  crewaiknowledgetest:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: crewaiknowledgetest
    environment:
      - AGENT_NAME=Crewaiknowledgetest
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
