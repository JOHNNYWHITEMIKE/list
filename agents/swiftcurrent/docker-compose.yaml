version: '3.8'

services:
  swiftcurrent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: swiftcurrent
    environment:
      - AGENT_NAME=Swiftcurrent
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
