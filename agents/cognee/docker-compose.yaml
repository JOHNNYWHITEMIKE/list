version: '3.8'

services:
  cognee:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cognee
    environment:
      - AGENT_NAME=Cognee
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
