version: '3.8'

services:
  python-agents:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: python-agents
    environment:
      - AGENT_NAME=Python-Agents
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
