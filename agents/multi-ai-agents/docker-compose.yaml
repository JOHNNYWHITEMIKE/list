version: '3.8'

services:
  multi-ai-agents:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: multi-ai-agents
    environment:
      - AGENT_NAME=Multi-Ai-Agents
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
