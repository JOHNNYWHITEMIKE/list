version: '3.8'

services:
  code-interpreter-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: code-interpreter-api
    environment:
      - AGENT_NAME=Code-Interpreter-Api
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
