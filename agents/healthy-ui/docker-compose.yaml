version: '3.8'

services:
  healthy-ui:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: healthy-ui
    environment:
      - AGENT_NAME=Healthy-Ui
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
