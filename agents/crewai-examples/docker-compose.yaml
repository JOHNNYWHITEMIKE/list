version: '3.8'

services:
  crewai-examples:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: crewai-examples
    environment:
      - AGENT_NAME=Crewai-Examples
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
