version: '3.8'

services:
  openai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: openai
    environment:
      - AGENT_NAME=OpenAI
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
