version: '3.8'

services:
  agentprocessingunit:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agentprocessingunit
    environment:
      - AGENT_NAME=Agentprocessingunit
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
