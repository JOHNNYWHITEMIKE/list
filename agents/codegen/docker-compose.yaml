version: '3.8'

services:
  codegen:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: codegen
    environment:
      - AGENT_NAME=Codegen
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
