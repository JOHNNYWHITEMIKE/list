version: '3.8'

services:
  stocks-ai-agent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: stocks-ai-agent
    environment:
      - AGENT_NAME=Stocks-Ai-Agent
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
