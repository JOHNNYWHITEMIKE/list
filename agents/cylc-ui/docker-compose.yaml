version: '3.8'

services:
  cylc-ui:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cylc-ui
    environment:
      - AGENT_NAME=Cylc-Ui
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
