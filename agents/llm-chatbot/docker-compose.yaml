version: '3.8'

services:
  llm-chatbot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llm-chatbot
    environment:
      - AGENT_NAME=Llm-Chatbot
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
