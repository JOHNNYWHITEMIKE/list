version: '3.8'

services:
  togetherai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: togetherai
    environment:
      - AGENT_NAME=TogetherAI
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
