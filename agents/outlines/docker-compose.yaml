version: '3.8'

services:
  outlines:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: outlines
    environment:
      - AGENT_NAME=Outlines
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
