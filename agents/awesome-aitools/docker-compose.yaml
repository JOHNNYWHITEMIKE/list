version: '3.8'

services:
  awesome-aitools:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: awesome-aitools
    environment:
      - AGENT_NAME=Awesome-Aitools
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
