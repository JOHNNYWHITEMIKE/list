version: '3.8'

services:
  tiny-agent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tiny-agent
    environment:
      - AGENT_NAME=Tiny-Agent
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
