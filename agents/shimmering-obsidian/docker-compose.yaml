version: '3.8'

services:
  shimmering-obsidian:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: shimmering-obsidian
    environment:
      - AGENT_NAME=Shimmering-Obsidian
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
