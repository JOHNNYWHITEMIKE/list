version: '3.8'

services:
  llmdataforge:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llmdataforge
    environment:
      - AGENT_NAME=Llmdataforge
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
