version: '3.8'

services:
  macllm:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: macllm
    environment:
      - AGENT_NAME=Macllm
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
