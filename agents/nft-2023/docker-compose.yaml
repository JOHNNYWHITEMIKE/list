version: '3.8'

services:
  nft-2023:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nft-2023
    environment:
      - AGENT_NAME=Nft-2023
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
