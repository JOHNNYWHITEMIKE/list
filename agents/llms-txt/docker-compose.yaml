version: '3.8'

services:
  llms-txt:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llms-txt
    environment:
      - AGENT_NAME=Llms-Txt
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
