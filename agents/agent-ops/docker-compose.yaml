version: '3.8'

services:
  agent-ops:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agent-ops
    environment:
      - AGENT_NAME=Agent-Ops
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
