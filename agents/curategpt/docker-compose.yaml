version: '3.8'

services:
  curategpt:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: curategpt
    environment:
      - AGENT_NAME=Curategpt
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
