version: '3.8'

services:
  spacecreateai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: spacecreateai
    environment:
      - AGENT_NAME=Spacecreateai
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
