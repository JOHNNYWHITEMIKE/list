version: '3.8'

services:
  ethics-tree:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ethics-tree
    environment:
      - AGENT_NAME=Ethics-Tree
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
