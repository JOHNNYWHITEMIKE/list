version: '3.8'

services:
  pruneme:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pruneme
    environment:
      - AGENT_NAME=Pruneme
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
