version: '3.8'

services:
  powerupsai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: powerupsai
    environment:
      - AGENT_NAME=Powerups.Ai
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
