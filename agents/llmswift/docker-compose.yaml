version: '3.8'

services:
  llmswift:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llmswift
    environment:
      - AGENT_NAME=Llm.Swift
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
