version: '3.8'

services:
  bazed:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bazed
    environment:
      - AGENT_NAME=Bazed
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
