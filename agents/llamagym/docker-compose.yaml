version: '3.8'

services:
  llamagym:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llamagym
    environment:
      - AGENT_NAME=LlamaGym
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
