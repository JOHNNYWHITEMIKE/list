version: '3.8'

services:
  enact:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: enact
    environment:
      - AGENT_NAME=Enact
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
