version: '3.8'

services:
  autospec:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: autospec
    environment:
      - AGENT_NAME=Autospec
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
