version: '3.8'

services:
  ai-gpt-agent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-gpt-agent
    environment:
      - AGENT_NAME=Ai-Gpt-Agent
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
