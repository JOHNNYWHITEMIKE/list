version: '3.8'

services:
  langkit:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: langkit
    environment:
      - AGENT_NAME=Langkit
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
