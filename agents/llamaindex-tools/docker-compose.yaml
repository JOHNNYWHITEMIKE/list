version: '3.8'

services:
  llamaindex-tools:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llamaindex-tools
    environment:
      - AGENT_NAME=LlamaIndex Tools
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
