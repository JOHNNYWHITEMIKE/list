version: '3.8'

services:
  web-llm-chat:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: web-llm-chat
    environment:
      - AGENT_NAME=Web-Llm-Chat
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
