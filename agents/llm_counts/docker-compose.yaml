version: '3.8'

services:
  llm_counts:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llm_counts
    environment:
      - AGENT_NAME=Llm_Counts
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
