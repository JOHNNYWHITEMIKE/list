version: '3.8'

services:
  ethicsinai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ethicsinai
    environment:
      - AGENT_NAME=Ethicsinai
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
