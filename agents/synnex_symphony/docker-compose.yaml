version: '3.8'

services:
  synnex_symphony:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: synnex_symphony
    environment:
      - AGENT_NAME=Synnex_Symphony
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
