version: '3.8'

services:
  nl2iac:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nl2iac
    environment:
      - AGENT_NAME=Nl2Iac
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
