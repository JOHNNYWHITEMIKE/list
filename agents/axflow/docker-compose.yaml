version: '3.8'

services:
  axflow:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: axflow
    environment:
      - AGENT_NAME=Axflow
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
