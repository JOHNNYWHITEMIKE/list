version: '3.8'

services:
  openpipe:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: openpipe
    environment:
      - AGENT_NAME=OpenPipe
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
