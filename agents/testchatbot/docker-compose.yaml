version: '3.8'

services:
  testchatbot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: testchatbot
    environment:
      - AGENT_NAME=Testchatbot
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
