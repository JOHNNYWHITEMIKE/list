version: '3.8'

services:
  machine-learning-examples:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: machine-learning-examples
    environment:
      - AGENT_NAME=Machine-Learning-Examples
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
