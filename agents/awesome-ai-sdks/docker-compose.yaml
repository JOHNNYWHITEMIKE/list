version: '3.8'

services:
  awesome-ai-sdks:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: awesome-ai-sdks
    environment:
      - AGENT_NAME=Awesome-Ai-Sdks
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
