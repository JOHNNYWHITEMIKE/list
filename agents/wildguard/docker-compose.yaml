version: '3.8'

services:
  wildguard:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: wildguard
    environment:
      - AGENT_NAME=Wildguard
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
