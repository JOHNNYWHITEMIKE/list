version: '3.8'

services:
  attendone:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: attendone
    environment:
      - AGENT_NAME=Attendone
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
