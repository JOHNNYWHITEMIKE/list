version: '3.8'

services:
  llmroofline:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llmroofline
    environment:
      - AGENT_NAME=Llmroofline
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
