version: '3.8'

services:
  awesome-llm-os:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: awesome-llm-os
    environment:
      - AGENT_NAME=Awesome-Llm-Os
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
