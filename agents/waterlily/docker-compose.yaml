version: '3.8'

services:
  waterlily:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: waterlily
    environment:
      - AGENT_NAME=Waterlily
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
