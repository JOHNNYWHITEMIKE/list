version: '3.8'

services:
  prelude:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: prelude
    environment:
      - AGENT_NAME=Prelude
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
