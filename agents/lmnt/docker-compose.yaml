version: '3.8'

services:
  lmnt:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: lmnt
    environment:
      - AGENT_NAME=LMNT
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
