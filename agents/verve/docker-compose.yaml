version: '3.8'

services:
  verve:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: verve
    environment:
      - AGENT_NAME=Verve
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
