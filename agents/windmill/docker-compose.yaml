version: '3.8'

services:
  windmill:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: windmill
    environment:
      - AGENT_NAME=Windmill
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
