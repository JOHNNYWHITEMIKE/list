version: '3.8'

services:
  swarmsxgcp:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: swarmsxgcp
    environment:
      - AGENT_NAME=Swarmsxgcp
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
