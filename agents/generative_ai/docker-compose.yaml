version: '3.8'

services:
  generative_ai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: generative_ai
    environment:
      - AGENT_NAME=Generative_Ai
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
