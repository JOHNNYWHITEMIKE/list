version: '3.8'

services:
  collective:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: collective
    environment:
      - AGENT_NAME=Collective
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
