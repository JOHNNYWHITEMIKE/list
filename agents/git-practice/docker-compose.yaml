version: '3.8'

services:
  git-practice:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: git-practice
    environment:
      - AGENT_NAME=Git-Practice
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
