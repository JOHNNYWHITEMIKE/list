version: '3.8'

services:
  ramp-workflow:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ramp-workflow
    environment:
      - AGENT_NAME=Ramp-Workflow
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
