version: '3.8'

services:
  agent-protocol:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agent-protocol
    environment:
      - AGENT_NAME=Agent Protocol
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
