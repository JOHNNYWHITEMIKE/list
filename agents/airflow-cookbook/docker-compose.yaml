version: '3.8'

services:
  airflow-cookbook:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: airflow-cookbook
    environment:
      - AGENT_NAME=Airflow-Cookbook
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
