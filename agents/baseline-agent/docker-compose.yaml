version: '3.8'

services:
  baseline-agent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: baseline-agent
    environment:
      - AGENT_NAME=Baseline-Agent
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
