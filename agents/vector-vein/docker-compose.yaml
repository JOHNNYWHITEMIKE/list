version: '3.8'

services:
  vector-vein:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vector-vein
    environment:
      - AGENT_NAME=Vector-Vein
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
