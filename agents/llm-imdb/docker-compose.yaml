version: '3.8'

services:
  llm-imdb:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llm-imdb
    environment:
      - AGENT_NAME=Llm-Imdb
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
