version: '3.8'

services:
  agileagents:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agileagents
    environment:
      - AGENT_NAME=Agileagents
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
