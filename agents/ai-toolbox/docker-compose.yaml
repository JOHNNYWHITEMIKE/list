version: '3.8'

services:
  ai-toolbox:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-toolbox
    environment:
      - AGENT_NAME=Ai-Toolbox
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
