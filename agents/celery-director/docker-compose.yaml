version: '3.8'

services:
  celery-director:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: celery-director
    environment:
      - AGENT_NAME=Celery-Director
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
