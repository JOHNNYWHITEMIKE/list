version: '3.8'

services:
  upar5iv:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: upar5iv
    environment:
      - AGENT_NAME=Upar5Iv
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
