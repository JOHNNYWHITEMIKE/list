version: '3.8'

services:
  speechllm:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: speechllm
    environment:
      - AGENT_NAME=Speechllm
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
