version: '3.8'

services:
  stock-analysis-with-llm:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: stock-analysis-with-llm
    environment:
      - AGENT_NAME=Stock-Analysis-With-Llm
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
