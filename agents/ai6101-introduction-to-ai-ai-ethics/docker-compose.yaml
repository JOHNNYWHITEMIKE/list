version: '3.8'

services:
  ai6101-introduction-to-ai-ai-ethics:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai6101-introduction-to-ai-ai-ethics
    environment:
      - AGENT_NAME=Ai6101-Introduction-To-Ai-Ai-Ethics
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
