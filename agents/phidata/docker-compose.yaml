version: '3.8'

services:
  phidata:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: phidata
    environment:
      - AGENT_NAME=Phidata
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
