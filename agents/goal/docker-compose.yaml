version: '3.8'

services:
  goal:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: goal
    environment:
      - AGENT_NAME=Goal
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
