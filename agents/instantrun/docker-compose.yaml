version: '3.8'

services:
  instantrun:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: instantrun
    environment:
      - AGENT_NAME=Instantrun
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
