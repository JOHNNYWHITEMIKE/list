version: '3.8'

services:
  chatgpt-prompt-engineering:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: chatgpt-prompt-engineering
    environment:
      - AGENT_NAME=Chatgpt-Prompt-Engineering
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
