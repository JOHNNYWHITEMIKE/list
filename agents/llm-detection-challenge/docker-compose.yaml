version: '3.8'

services:
  llm-detection-challenge:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llm-detection-challenge
    environment:
      - AGENT_NAME=Llm-Detection-Challenge
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
