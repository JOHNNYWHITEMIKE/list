version: '3.8'

services:
  cody:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cody
    environment:
      - AGENT_NAME=Cody
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
