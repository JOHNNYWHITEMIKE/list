version: '3.8'

services:
  purpaas-llm:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: purpaas-llm
    environment:
      - AGENT_NAME=Purpaas-Llm
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
