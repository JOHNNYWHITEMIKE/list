version: '3.8'

services:
  airmageddon:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: airmageddon
    environment:
      - AGENT_NAME=Airmageddon
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
