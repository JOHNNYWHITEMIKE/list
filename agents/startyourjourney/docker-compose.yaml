version: '3.8'

services:
  startyourjourney:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: startyourjourney
    environment:
      - AGENT_NAME=Startyourjourney
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
