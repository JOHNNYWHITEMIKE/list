version: '3.8'

services:
  dinersim:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dinersim
    environment:
      - AGENT_NAME=Dinersim
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
