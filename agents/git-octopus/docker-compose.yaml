version: '3.8'

services:
  git-octopus:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: git-octopus
    environment:
      - AGENT_NAME=Git-Octopus
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
