version: '3.8'

services:
  potpie:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: potpie
    environment:
      - AGENT_NAME=Potpie
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
