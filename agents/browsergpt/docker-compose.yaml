version: '3.8'

services:
  browsergpt:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: browsergpt
    environment:
      - AGENT_NAME=BrowserGPT
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
