version: '3.8'

services:
  cheap_ai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cheap_ai
    environment:
      - AGENT_NAME=Cheap_Ai
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
