version: '3.8'

services:
  bktask:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bktask
    environment:
      - AGENT_NAME=Bktask
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
