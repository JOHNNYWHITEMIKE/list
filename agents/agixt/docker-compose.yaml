version: '3.8'

services:
  agixt:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agixt
    environment:
      - AGENT_NAME=AGiXT
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
