version: '3.8'

services:
  ai-powered-chatbot-generator:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-powered-chatbot-generator
    environment:
      - AGENT_NAME=Ai-Powered-Chatbot-Generator
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
