version: '3.8'

services:
  llm-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llm-app
    environment:
      - AGENT_NAME=Llm-App
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
