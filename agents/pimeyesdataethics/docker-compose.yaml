version: '3.8'

services:
  pimeyesdataethics:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pimeyesdataethics
    environment:
      - AGENT_NAME=Pimeyesdataethics
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
