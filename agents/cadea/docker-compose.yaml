version: '3.8'

services:
  cadea:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cadea
    environment:
      - AGENT_NAME=Cadea
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
