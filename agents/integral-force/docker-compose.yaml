version: '3.8'

services:
  integral-force:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: integral-force
    environment:
      - AGENT_NAME=Integral-Force
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
