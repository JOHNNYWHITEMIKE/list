version: '3.8'

services:
  llm-finetune:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llm-finetune
    environment:
      - AGENT_NAME=Llm-Finetune
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
