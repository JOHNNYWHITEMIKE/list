version: '3.8'

services:
  autogenbook:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: autogenbook
    environment:
      - AGENT_NAME=Autogenbook
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
