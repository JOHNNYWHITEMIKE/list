version: '3.8'

services:
  learn_prompting:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: learn_prompting
    environment:
      - AGENT_NAME=Learn_Prompting
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
