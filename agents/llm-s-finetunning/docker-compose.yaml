version: '3.8'

services:
  llm-s-finetunning:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llm-s-finetunning
    environment:
      - AGENT_NAME=Llm-S-Finetunning
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
