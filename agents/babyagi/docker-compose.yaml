version: '3.8'

services:
  babyagi:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: babyagi
    environment:
      - AGENT_NAME=BabyAGI
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
