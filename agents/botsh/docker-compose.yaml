version: '3.8'

services:
  botsh:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: botsh
    environment:
      - AGENT_NAME=Botsh
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
