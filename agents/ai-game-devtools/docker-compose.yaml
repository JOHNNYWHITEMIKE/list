version: '3.8'

services:
  ai-game-devtools:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-game-devtools
    environment:
      - AGENT_NAME=Ai-Game-Devtools
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
