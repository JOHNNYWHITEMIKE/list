version: '3.8'

services:
  tideflow:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tideflow
    environment:
      - AGENT_NAME=Tideflow
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
