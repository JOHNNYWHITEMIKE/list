version: '3.8'

services:
  aigent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aigent
    environment:
      - AGENT_NAME=Aigent
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
