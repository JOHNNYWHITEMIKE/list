version: '3.8'

services:
  finetuning_llm:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: finetuning_llm
    environment:
      - AGENT_NAME=Finetuning_Llm
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
