version: '3.8'

services:
  cosmo:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cosmo
    environment:
      - AGENT_NAME=Cosmo
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
