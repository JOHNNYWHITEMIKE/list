version: '3.8'

services:
  rptq4llm:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rptq4llm
    environment:
      - AGENT_NAME=Rptq4Llm
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
