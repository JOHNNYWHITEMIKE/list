version: '3.8'

services:
  nactivity:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nactivity
    environment:
      - AGENT_NAME=Nactivity
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
