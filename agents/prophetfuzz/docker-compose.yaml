version: '3.8'

services:
  prophetfuzz:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: prophetfuzz
    environment:
      - AGENT_NAME=Prophetfuzz
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
