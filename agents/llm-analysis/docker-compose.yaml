version: '3.8'

services:
  llm-analysis:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llm-analysis
    environment:
      - AGENT_NAME=Llm-Analysis
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
