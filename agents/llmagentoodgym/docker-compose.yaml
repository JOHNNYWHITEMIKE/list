version: '3.8'

services:
  llmagentoodgym:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llmagentoodgym
    environment:
      - AGENT_NAME=Llmagentoodgym
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
