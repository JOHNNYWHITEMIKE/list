version: '3.8'

services:
  nest-ai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nest-ai
    environment:
      - AGENT_NAME=Nest-Ai
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
