version: '3.8'

services:
  anybiz:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: anybiz
    environment:
      - AGENT_NAME=AnyBiz
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
