version: '3.8'

services:
  mammoth:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mammoth
    environment:
      - AGENT_NAME=Mammoth
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
