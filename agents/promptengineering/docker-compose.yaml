version: '3.8'

services:
  promptengineering:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: promptengineering
    environment:
      - AGENT_NAME=Promptengineering
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
