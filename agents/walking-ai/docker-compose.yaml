version: '3.8'

services:
  walking-ai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: walking-ai
    environment:
      - AGENT_NAME=Walking-Ai
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
