version: '3.8'

services:
  agenticmd:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agenticmd
    environment:
      - AGENT_NAME=Agentic.Md
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
