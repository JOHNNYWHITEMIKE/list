version: '3.8'

services:
  abc-classroom:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: abc-classroom
    environment:
      - AGENT_NAME=Abc-Classroom
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
