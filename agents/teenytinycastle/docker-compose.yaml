version: '3.8'

services:
  teenytinycastle:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: teenytinycastle
    environment:
      - AGENT_NAME=Teenytinycastle
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
