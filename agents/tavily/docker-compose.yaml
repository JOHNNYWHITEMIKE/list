version: '3.8'

services:
  tavily:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tavily
    environment:
      - AGENT_NAME=Tavily
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
