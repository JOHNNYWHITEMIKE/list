version: '3.8'

services:
  llmgnn:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llmgnn
    environment:
      - AGENT_NAME=Llmgnn
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
