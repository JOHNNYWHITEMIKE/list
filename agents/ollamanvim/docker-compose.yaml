version: '3.8'

services:
  ollamanvim:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ollamanvim
    environment:
      - AGENT_NAME=Ollama.Nvim
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
