version: '3.8'

services:
  swarmnode-go:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: swarmnode-go
    environment:
      - AGENT_NAME=Swarmnode-Go
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
