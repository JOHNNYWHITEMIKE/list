version: '3.8'

services:
  devstream:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: devstream
    environment:
      - AGENT_NAME=Devstream
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
