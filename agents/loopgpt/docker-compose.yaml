version: '3.8'

services:
  loopgpt:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: loopgpt
    environment:
      - AGENT_NAME=LoopGPT
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
