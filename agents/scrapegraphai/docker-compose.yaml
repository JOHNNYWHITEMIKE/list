version: '3.8'

services:
  scrapegraphai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: scrapegraphai
    environment:
      - AGENT_NAME=ScrapeGraphAI
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
