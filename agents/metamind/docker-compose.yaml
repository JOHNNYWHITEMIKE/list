version: '3.8'

services:
  metamind:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: metamind
    environment:
      - AGENT_NAME=Metamind
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
