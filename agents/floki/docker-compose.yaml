version: '3.8'

services:
  floki:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: floki
    environment:
      - AGENT_NAME=Floki
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
