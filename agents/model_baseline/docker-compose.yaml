version: '3.8'

services:
  model_baseline:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: model_baseline
    environment:
      - AGENT_NAME=Model_Baseline
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
