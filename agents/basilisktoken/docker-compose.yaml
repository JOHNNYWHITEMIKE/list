version: '3.8'

services:
  basilisktoken:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: basilisktoken
    environment:
      - AGENT_NAME=Basilisktoken
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
