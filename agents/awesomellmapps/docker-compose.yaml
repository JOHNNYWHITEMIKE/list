version: '3.8'

services:
  awesomellmapps:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: awesomellmapps
    environment:
      - AGENT_NAME=Awesomellmapps
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
