version: '3.8'

services:
  resources:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: resources
    environment:
      - AGENT_NAME=Resources
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
