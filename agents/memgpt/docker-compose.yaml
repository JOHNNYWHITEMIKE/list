version: '3.8'

services:
  memgpt:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: memgpt
    environment:
      - AGENT_NAME=MemGPT
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
