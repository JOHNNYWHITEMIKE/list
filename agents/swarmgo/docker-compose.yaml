version: '3.8'

services:
  swarmgo:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: swarmgo
    environment:
      - AGENT_NAME=Swarmgo
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
