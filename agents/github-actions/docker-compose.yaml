version: '3.8'

services:
  github-actions:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: github-actions
    environment:
      - AGENT_NAME=Github-Actions
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
