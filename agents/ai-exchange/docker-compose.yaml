version: '3.8'

services:
  ai-exchange:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-exchange
    environment:
      - AGENT_NAME=Ai-Exchange
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
