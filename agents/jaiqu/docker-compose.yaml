version: '3.8'

services:
  jaiqu:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: jaiqu
    environment:
      - AGENT_NAME=Jaiqu
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
