version: '3.8'

services:
  azkaban:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: azkaban
    environment:
      - AGENT_NAME=Azkaban
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
