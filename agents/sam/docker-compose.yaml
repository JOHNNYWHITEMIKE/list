version: '3.8'

services:
  sam:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sam
    environment:
      - AGENT_NAME=Sam
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
