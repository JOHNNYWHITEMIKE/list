version: '3.8'

services:
  pitagora-cwl:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pitagora-cwl
    environment:
      - AGENT_NAME=Pitagora-Cwl
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
