version: '3.8'

services:
  rime-ai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rime-ai
    environment:
      - AGENT_NAME=Rime AI
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
