version: '3.8'

services:
  llm_benchmarks:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llm_benchmarks
    environment:
      - AGENT_NAME=Llm_Benchmarks
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
