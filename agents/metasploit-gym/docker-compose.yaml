version: '3.8'

services:
  metasploit-gym:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: metasploit-gym
    environment:
      - AGENT_NAME=Metasploit-Gym
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
