version: '3.8'

services:
  agentos:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agentos
    environment:
      - AGENT_NAME=Agentos
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
