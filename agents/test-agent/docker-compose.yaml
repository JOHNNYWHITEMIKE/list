version: '3.8'

services:
  test-agent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: test-agent
    environment:
      - AGENT_NAME=Test-Agent
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
