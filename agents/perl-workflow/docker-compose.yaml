version: '3.8'

services:
  perl-workflow:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: perl-workflow
    environment:
      - AGENT_NAME=Perl-Workflow
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
