version: '3.8'

services:
  instrukt:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: instrukt
    environment:
      - AGENT_NAME=Instrukt
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
