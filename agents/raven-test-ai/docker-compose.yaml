version: '3.8'

services:
  raven-test-ai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: raven-test-ai
    environment:
      - AGENT_NAME=Raven-Test-Ai
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
