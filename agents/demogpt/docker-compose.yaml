version: '3.8'

services:
  demogpt:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: demogpt
    environment:
      - AGENT_NAME=Demogpt
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
