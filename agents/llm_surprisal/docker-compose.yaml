version: '3.8'

services:
  llm_surprisal:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llm_surprisal
    environment:
      - AGENT_NAME=Llm_Surprisal
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
