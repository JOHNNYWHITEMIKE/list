version: '3.8'

services:
  playai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: playai
    environment:
      - AGENT_NAME=PlayAI
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
