version: '3.8'

services:
  zapier:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: zapier
    environment:
      - AGENT_NAME=Zapier
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
