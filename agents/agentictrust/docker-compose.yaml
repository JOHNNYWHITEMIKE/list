version: '3.8'

services:
  agentictrust:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agentictrust
    environment:
      - AGENT_NAME=Agentictrust
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
