version: '3.8'

services:
  selenium-agent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: selenium-agent
    environment:
      - AGENT_NAME=Selenium-Agent
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
