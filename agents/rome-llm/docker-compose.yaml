version: '3.8'

services:
  rome-llm:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rome-llm
    environment:
      - AGENT_NAME=Rome-Llm
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
