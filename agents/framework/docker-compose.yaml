version: '3.8'

services:
  framework:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: framework
    environment:
      - AGENT_NAME=Framework
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
