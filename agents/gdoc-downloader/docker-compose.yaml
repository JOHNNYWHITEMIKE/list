version: '3.8'

services:
  gdoc-downloader:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: gdoc-downloader
    environment:
      - AGENT_NAME=Gdoc-Downloader
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
