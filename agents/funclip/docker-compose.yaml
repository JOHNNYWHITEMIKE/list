version: '3.8'

services:
  funclip:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: funclip
    environment:
      - AGENT_NAME=Funclip
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
