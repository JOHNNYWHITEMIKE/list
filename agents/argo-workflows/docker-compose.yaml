version: '3.8'

services:
  argo-workflows:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: argo-workflows
    environment:
      - AGENT_NAME=Argo-Workflows
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
