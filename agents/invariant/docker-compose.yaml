version: '3.8'

services:
  invariant:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: invariant
    environment:
      - AGENT_NAME=Invariant
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
