version: '3.8'

services:
  openrouter:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: openrouter
    environment:
      - AGENT_NAME=OpenRouter
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
