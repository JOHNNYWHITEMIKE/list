version: '3.8'

services:
  draft:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: draft
    environment:
      - AGENT_NAME=Draft
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
