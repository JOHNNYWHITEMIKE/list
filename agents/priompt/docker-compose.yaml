version: '3.8'

services:
  priompt:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: priompt
    environment:
      - AGENT_NAME=Priompt
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
