version: '3.8'

services:
  ai-writing-survey:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-writing-survey
    environment:
      - AGENT_NAME=Ai-Writing-Survey
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
