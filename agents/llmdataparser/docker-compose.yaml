version: '3.8'

services:
  llmdataparser:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llmdataparser
    environment:
      - AGENT_NAME=Llmdataparser
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
