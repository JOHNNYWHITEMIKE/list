version: '3.8'

services:
  talkscriber:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: talkscriber
    environment:
      - AGENT_NAME=Talkscriber
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
