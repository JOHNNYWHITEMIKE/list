version: '3.8'

services:
  smriprep:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: smriprep
    environment:
      - AGENT_NAME=Smriprep
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
