version: '3.8'

services:
  starjob:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: starjob
    environment:
      - AGENT_NAME=Starjob
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
