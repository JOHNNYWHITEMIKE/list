version: '3.8'

services:
  sop-bench:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sop-bench
    environment:
      - AGENT_NAME=Sop-Bench
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
