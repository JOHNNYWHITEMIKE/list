version: '3.8'

services:
  baba-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: baba-bot
    environment:
      - AGENT_NAME=Baba-Bot
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
