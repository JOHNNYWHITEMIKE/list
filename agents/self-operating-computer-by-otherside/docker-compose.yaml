version: '3.8'

services:
  self-operating-computer-by-otherside:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: self-operating-computer-by-otherside
    environment:
      - AGENT_NAME=Self Operating Computer by Otherside
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
