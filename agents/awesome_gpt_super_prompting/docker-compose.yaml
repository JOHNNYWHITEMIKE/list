version: '3.8'

services:
  awesome_gpt_super_prompting:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: awesome_gpt_super_prompting
    environment:
      - AGENT_NAME=Awesome_Gpt_Super_Prompting
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
