version: '3.8'

services:
  chartgpt:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: chartgpt
    environment:
      - AGENT_NAME=ChartGPT
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
