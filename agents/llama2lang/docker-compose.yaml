version: '3.8'

services:
  llama2lang:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llama2lang
    environment:
      - AGENT_NAME=Llama2Lang
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
