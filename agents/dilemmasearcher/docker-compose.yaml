version: '3.8'

services:
  dilemmasearcher:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dilemmasearcher
    environment:
      - AGENT_NAME=Dilemmasearcher
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
