version: '3.8'

services:
  blockchain-ethics:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: blockchain-ethics
    environment:
      - AGENT_NAME=Blockchain-Ethics
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
