version: '3.8'

services:
  mobilebench:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mobilebench
    environment:
      - AGENT_NAME=Mobilebench
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
