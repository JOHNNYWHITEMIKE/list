version: '3.8'

services:
  sdm23:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sdm23
    environment:
      - AGENT_NAME=Sdm23
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
