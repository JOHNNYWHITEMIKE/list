version: '3.8'

services:
  composio:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: composio
    environment:
      - AGENT_NAME=Composio
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
