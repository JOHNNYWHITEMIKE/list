version: '3.8'

services:
  ai_tools:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai_tools
    environment:
      - AGENT_NAME=Ai_Tools
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
