version: '3.8'

services:
  nak-aiethics:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nak-aiethics
    environment:
      - AGENT_NAME=Nak-Aiethics
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
