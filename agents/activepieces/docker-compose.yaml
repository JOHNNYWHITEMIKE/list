version: '3.8'

services:
  activepieces:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: activepieces
    environment:
      - AGENT_NAME=Activepieces
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
