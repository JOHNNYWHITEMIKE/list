version: '3.8'

services:
  aidialer:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aidialer
    environment:
      - AGENT_NAME=Aidialer
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
