version: '3.8'

services:
  auto-gpt:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: auto-gpt
    environment:
      - AGENT_NAME=Auto-GPT
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
