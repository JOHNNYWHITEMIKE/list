version: '3.8'

services:
  langfuse:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: langfuse
    environment:
      - AGENT_NAME=Langfuse
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
