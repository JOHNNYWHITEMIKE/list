version: '3.8'

services:
  miranda:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: miranda
    environment:
      - AGENT_NAME=Miranda
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
