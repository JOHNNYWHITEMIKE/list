version: '3.8'

services:
  localgpt:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: localgpt
    environment:
      - AGENT_NAME=LocalGPT
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
