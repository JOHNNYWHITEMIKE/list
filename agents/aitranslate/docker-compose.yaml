version: '3.8'

services:
  aitranslate:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aitranslate
    environment:
      - AGENT_NAME=Aitranslate
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
