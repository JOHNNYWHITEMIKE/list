version: '3.8'

services:
  functionary:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: functionary
    environment:
      - AGENT_NAME=Functionary
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
