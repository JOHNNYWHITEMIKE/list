version: '3.8'

services:
  metagpt:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: metagpt
    environment:
      - AGENT_NAME=MetaGPT
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
