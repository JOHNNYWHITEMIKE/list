version: '3.8'

services:
  livekit-agents:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: livekit-agents
    environment:
      - AGENT_NAME=LiveKit Agents
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
