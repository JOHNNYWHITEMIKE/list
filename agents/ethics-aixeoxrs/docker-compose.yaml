version: '3.8'

services:
  ethics-aixeoxrs:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ethics-aixeoxrs
    environment:
      - AGENT_NAME=Ethics-Aixeoxrs
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
