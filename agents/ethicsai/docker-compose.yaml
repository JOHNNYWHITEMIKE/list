version: '3.8'

services:
  ethicsai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ethicsai
    environment:
      - AGENT_NAME=Ethicsai
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
