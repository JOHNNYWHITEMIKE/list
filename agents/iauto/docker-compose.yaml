version: '3.8'

services:
  iauto:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: iauto
    environment:
      - AGENT_NAME=Iauto
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
