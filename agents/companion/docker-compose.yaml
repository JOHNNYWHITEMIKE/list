version: '3.8'

services:
  companion:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: companion
    environment:
      - AGENT_NAME=Companion
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
