version: '3.8'

services:
  realmofrespect:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: realmofrespect
    environment:
      - AGENT_NAME=Realmofrespect
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
