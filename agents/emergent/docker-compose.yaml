version: '3.8'

services:
  emergent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: emergent
    environment:
      - AGENT_NAME=Emergent
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
