version: '3.8'

services:
  aimultiagents:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aimultiagents
    environment:
      - AGENT_NAME=Aimultiagents
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
