version: '3.8'

services:
  llm-finetuning:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llm-finetuning
    environment:
      - AGENT_NAME=Llm-Finetuning
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
