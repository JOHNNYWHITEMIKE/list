version: '3.8'

services:
  bland:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bland
    environment:
      - AGENT_NAME=Bland
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
