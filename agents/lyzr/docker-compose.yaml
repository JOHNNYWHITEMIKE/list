version: '3.8'

services:
  lyzr:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: lyzr
    environment:
      - AGENT_NAME=Lyzr
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
