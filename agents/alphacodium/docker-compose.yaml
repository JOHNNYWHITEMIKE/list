version: '3.8'

services:
  alphacodium:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: alphacodium
    environment:
      - AGENT_NAME=Alphacodium
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
