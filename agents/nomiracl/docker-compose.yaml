version: '3.8'

services:
  nomiracl:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nomiracl
    environment:
      - AGENT_NAME=Nomiracl
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
