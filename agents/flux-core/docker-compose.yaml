version: '3.8'

services:
  flux-core:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: flux-core
    environment:
      - AGENT_NAME=Flux-Core
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
