version: '3.8'

services:
  appworld:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: appworld
    environment:
      - AGENT_NAME=Appworld
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
