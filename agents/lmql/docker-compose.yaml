version: '3.8'

services:
  lmql:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: lmql
    environment:
      - AGENT_NAME=LMQL
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
