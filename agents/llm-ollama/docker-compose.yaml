version: '3.8'

services:
  llm-ollama:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llm-ollama
    environment:
      - AGENT_NAME=Llm-Ollama
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
