version: '3.8'

services:
  openai-tools:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: openai-tools
    environment:
      - AGENT_NAME=Openai-Tools
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
