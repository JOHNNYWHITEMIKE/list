version: '3.8'

services:
  lm-studio:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: lm-studio
    environment:
      - AGENT_NAME=LM Studio
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
