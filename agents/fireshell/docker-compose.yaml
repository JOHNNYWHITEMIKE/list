version: '3.8'

services:
  fireshell:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fireshell
    environment:
      - AGENT_NAME=Fireshell
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
