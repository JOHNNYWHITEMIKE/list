version: '3.8'

services:
  jest-ai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: jest-ai
    environment:
      - AGENT_NAME=Jest-Ai
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
