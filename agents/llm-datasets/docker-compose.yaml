version: '3.8'

services:
  llm-datasets:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llm-datasets
    environment:
      - AGENT_NAME=Llm-Datasets
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
