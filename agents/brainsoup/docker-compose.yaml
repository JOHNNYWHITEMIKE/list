version: '3.8'

services:
  brainsoup:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: brainsoup
    environment:
      - AGENT_NAME=BrainSoup
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
