version: '3.8'

services:
  markdown_llm:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: markdown_llm
    environment:
      - AGENT_NAME=Markdown_Llm
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
