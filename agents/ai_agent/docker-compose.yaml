version: '3.8'

services:
  ai_agent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai_agent
    environment:
      - AGENT_NAME=Ai_Agent
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
