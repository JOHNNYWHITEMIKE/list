version: '3.8'

services:
  just-eval:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: just-eval
    environment:
      - AGENT_NAME=Just-Eval
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
