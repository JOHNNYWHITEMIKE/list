version: '3.8'

services:
  jan:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: jan
    environment:
      - AGENT_NAME=Jan
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
