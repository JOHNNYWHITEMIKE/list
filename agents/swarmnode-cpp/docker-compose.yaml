version: '3.8'

services:
  swarmnode-cpp:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: swarmnode-cpp
    environment:
      - AGENT_NAME=Swarmnode-Cpp
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
