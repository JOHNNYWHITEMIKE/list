version: '3.8'

services:
  taskingai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: taskingai
    environment:
      - AGENT_NAME=TaskingAI
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
