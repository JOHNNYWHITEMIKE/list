version: '3.8'

services:
  llm_finetune:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llm_finetune
    environment:
      - AGENT_NAME=Llm_Finetune
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
