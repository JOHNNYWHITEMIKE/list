version: '3.8'

services:
  autonomousai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: autonomousai
    environment:
      - AGENT_NAME=Autonomousai
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
