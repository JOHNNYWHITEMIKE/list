version: '3.8'

services:
  mastermind:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mastermind
    environment:
      - AGENT_NAME=Mastermind
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
