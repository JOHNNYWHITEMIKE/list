version: '3.8'

services:
  ai-safety-ethics:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-safety-ethics
    environment:
      - AGENT_NAME=Ai-Safety-Ethics
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
