version: '3.8'

services:
  ftdatagen:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ftdatagen
    environment:
      - AGENT_NAME=Ftdatagen
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
