version: '3.8'

services:
  slashbase-go:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: slashbase-go
    environment:
      - AGENT_NAME=Slashbase-Go
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
