version: '3.8'

services:
  marvin:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: marvin
    environment:
      - AGENT_NAME=Marvin
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
