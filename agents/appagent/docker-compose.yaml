version: '3.8'

services:
  appagent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: appagent
    environment:
      - AGENT_NAME=AppAgent
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
