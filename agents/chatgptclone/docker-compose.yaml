version: '3.8'

services:
  chatgptclone:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: chatgptclone
    environment:
      - AGENT_NAME=Chatgpt.Clone
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
