version: '3.8'

services:
  ai-agent-lab:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-agent-lab
    environment:
      - AGENT_NAME=Ai-Agent-Lab
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
