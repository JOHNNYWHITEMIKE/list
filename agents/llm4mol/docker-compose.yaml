version: '3.8'

services:
  llm4mol:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llm4mol
    environment:
      - AGENT_NAME=Llm4Mol
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
