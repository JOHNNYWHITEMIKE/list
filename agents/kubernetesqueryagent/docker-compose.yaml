version: '3.8'

services:
  kubernetesqueryagent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: kubernetesqueryagent
    environment:
      - AGENT_NAME=Kubernetesqueryagent
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
