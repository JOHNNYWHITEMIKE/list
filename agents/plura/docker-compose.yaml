version: '3.8'

services:
  plura:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: plura
    environment:
      - AGENT_NAME=Plura
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
