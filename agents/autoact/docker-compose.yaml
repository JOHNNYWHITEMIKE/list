version: '3.8'

services:
  autoact:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: autoact
    environment:
      - AGENT_NAME=AutoAct
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
