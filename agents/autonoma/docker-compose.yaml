version: '3.8'

services:
  autonoma:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: autonoma
    environment:
      - AGENT_NAME=Autonoma
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
