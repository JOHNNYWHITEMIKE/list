version: '3.8'

services:
  d6tflow:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: d6tflow
    environment:
      - AGENT_NAME=D6Tflow
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
