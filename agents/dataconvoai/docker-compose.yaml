version: '3.8'

services:
  dataconvoai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dataconvoai
    environment:
      - AGENT_NAME=Dataconvoai
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
