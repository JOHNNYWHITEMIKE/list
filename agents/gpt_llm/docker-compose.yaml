version: '3.8'

services:
  gpt_llm:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: gpt_llm
    environment:
      - AGENT_NAME=Gpt_Llm
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
