version: '3.8'

services:
  llmguts:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llmguts
    environment:
      - AGENT_NAME=Llm.Guts
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
