version: '3.8'

services:
  freeze-omni:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: freeze-omni
    environment:
      - AGENT_NAME=Freeze-Omni
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
