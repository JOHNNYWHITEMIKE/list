version: '3.8'

services:
  hawk-projects:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: hawk-projects
    environment:
      - AGENT_NAME=Hawk-Projects
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
