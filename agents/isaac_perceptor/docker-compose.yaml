version: '3.8'

services:
  isaac_perceptor:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: isaac_perceptor
    environment:
      - AGENT_NAME=Isaac_Perceptor
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
