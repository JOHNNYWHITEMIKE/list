version: '3.8'

services:
  ai-agent-teams:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-agent-teams
    environment:
      - AGENT_NAME=Ai-Agent-Teams
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
