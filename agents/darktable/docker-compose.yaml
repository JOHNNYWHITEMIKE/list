version: '3.8'

services:
  darktable:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: darktable
    environment:
      - AGENT_NAME=Darktable
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
