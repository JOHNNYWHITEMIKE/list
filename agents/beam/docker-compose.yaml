version: '3.8'

services:
  beam:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: beam
    environment:
      - AGENT_NAME=Beam
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
