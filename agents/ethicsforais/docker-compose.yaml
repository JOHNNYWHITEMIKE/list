version: '3.8'

services:
  ethicsforais:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ethicsforais
    environment:
      - AGENT_NAME=Ethicsforais
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
