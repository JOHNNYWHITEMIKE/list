version: '3.8'

services:
  langchain-tools:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: langchain-tools
    environment:
      - AGENT_NAME=LangChain Tools
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
