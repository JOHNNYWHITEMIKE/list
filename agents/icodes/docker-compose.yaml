version: '3.8'

services:
  icodes:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: icodes
    environment:
      - AGENT_NAME=Icodes
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
