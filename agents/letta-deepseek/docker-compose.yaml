version: '3.8'

services:
  letta-deepseek:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: letta-deepseek
    environment:
      - AGENT_NAME=Letta-Deepseek
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
