version: '3.8'

services:
  vlm_databuilder:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vlm_databuilder
    environment:
      - AGENT_NAME=Vlm_Databuilder
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
