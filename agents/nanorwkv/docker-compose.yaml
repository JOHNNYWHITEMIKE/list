version: '3.8'

services:
  nanorwkv:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nanorwkv
    environment:
      - AGENT_NAME=Nanorwkv
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
