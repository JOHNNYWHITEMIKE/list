version: '3.8'

services:
  lunaai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: lunaai
    environment:
      - AGENT_NAME=Lunaai
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
