version: '3.8'

services:
  comfyuimini:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: comfyuimini
    environment:
      - AGENT_NAME=Comfyuimini
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
