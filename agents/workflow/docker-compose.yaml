version: '3.8'

services:
  workflow:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: workflow
    environment:
      - AGENT_NAME=Workflow
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
