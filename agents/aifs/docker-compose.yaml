version: '3.8'

services:
  aifs:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aifs
    environment:
      - AGENT_NAME=aifs
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
