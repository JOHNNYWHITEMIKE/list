version: '3.8'

services:
  wordpressify:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: wordpressify
    environment:
      - AGENT_NAME=Wordpressify
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
