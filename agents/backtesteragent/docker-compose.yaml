version: '3.8'

services:
  backtesteragent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: backtesteragent
    environment:
      - AGENT_NAME=Backtesteragent
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
