version: '3.8'

services:
  pipenv:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pipenv
    environment:
      - AGENT_NAME=Pipenv
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
