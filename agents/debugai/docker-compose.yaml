version: '3.8'

services:
  debugai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: debugai
    environment:
      - AGENT_NAME=Debugai
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
