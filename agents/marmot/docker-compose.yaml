version: '3.8'

services:
  marmot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: marmot
    environment:
      - AGENT_NAME=Marmot
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
