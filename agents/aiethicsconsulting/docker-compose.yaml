version: '3.8'

services:
  aiethicsconsulting:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aiethicsconsulting
    environment:
      - AGENT_NAME=Aiethicsconsulting
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
