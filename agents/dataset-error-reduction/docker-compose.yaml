version: '3.8'

services:
  dataset-error-reduction:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dataset-error-reduction
    environment:
      - AGENT_NAME=Dataset-Error-Reduction
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
