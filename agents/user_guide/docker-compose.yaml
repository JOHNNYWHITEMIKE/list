version: '3.8'

services:
  user_guide:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: user_guide
    environment:
      - AGENT_NAME=User_Guide
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
