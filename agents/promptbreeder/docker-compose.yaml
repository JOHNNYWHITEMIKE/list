version: '3.8'

services:
  promptbreeder:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: promptbreeder
    environment:
      - AGENT_NAME=Promptbreeder
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
