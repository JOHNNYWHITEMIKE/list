version: '3.8'

services:
  weblinx:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: weblinx
    environment:
      - AGENT_NAME=Weblinx
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
