version: '3.8'

services:
  jsonify:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: jsonify
    environment:
      - AGENT_NAME=Jsonify
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
