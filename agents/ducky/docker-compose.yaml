version: '3.8'

services:
  ducky:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ducky
    environment:
      - AGENT_NAME=Ducky
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
