version: '3.8'

services:
  elevenlabs:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: elevenlabs
    environment:
      - AGENT_NAME=ElevenLabs
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
