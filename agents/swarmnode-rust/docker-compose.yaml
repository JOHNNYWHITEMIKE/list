version: '3.8'

services:
  swarmnode-rust:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: swarmnode-rust
    environment:
      - AGENT_NAME=Swarmnode-Rust
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
