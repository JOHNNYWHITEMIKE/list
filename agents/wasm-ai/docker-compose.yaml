version: '3.8'

services:
  wasm-ai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: wasm-ai
    environment:
      - AGENT_NAME=Wasm-Ai
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
