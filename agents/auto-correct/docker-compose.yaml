version: '3.8'

services:
  auto-correct:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: auto-correct
    environment:
      - AGENT_NAME=Auto-Correct
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
