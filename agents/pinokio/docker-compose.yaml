version: '3.8'

services:
  pinokio:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pinokio
    environment:
      - AGENT_NAME=Pinokio
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
