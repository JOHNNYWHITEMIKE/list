version: '3.8'

services:
  fingen:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fingen
    environment:
      - AGENT_NAME=FinGen
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
