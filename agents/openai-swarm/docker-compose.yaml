version: '3.8'

services:
  openai-swarm:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: openai-swarm
    environment:
      - AGENT_NAME=OpenAI Swarm
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
