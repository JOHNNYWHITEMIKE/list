version: '3.8'

services:
  llm-slackbot-channels:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llm-slackbot-channels
    environment:
      - AGENT_NAME=Llm-Slackbot-Channels
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
