version: '3.8'

services:
  01:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: 01
    environment:
      - AGENT_NAME=01
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
