version: '3.8'

services:
  optix-toolkit:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: optix-toolkit
    environment:
      - AGENT_NAME=Optix-Toolkit
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
