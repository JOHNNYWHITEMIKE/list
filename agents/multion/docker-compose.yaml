version: '3.8'

services:
  multion:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: multion
    environment:
      - AGENT_NAME=MultiOn
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
