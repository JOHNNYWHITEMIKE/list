version: '3.8'

services:
  crewai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: crewai
    environment:
      - AGENT_NAME=Crewai
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
