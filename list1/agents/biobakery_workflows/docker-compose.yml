version: '3.8'

services:
  biobakery_workflows:
    build: .
    container_name: biobakery_workflows
    environment:
      - AGENT_NAME=biobakery_workflows
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
