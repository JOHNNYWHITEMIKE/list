version: '3.8'

services:
  agent-ops:
    build: .
    container_name: agent-ops
    environment:
      - AGENT_NAME=agent-ops
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
