version: '3.8'

services:
  hackopenaisystemprompts:
    build: .
    container_name: hackopenaisystemprompts
    environment:
      - AGENT_NAME=hackopenaisystemprompts
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
