version: '3.8'

services:
  aider:
    build: .
    container_name: aider
    environment:
      - AGENT_NAME=aider
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
