version: '3.8'

services:
  funclip:
    build: .
    container_name: funclip
    environment:
      - AGENT_NAME=funclip
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
