version: '3.8'

services:
  ai-agent-wallet:
    build: .
    container_name: ai-agent-wallet
    environment:
      - AGENT_NAME=ai-agent-wallet
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
