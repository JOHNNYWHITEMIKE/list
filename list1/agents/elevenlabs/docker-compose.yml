version: '3.8'

services:
  elevenlabs:
    build: .
    container_name: elevenlabs
    environment:
      - AGENT_NAME=elevenlabs
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
