version: '3.8'

services:
  draft:
    build: .
    container_name: draft
    environment:
      - AGENT_NAME=draft
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
