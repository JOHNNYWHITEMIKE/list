version: '3.8'

services:
  ai-artifacts:
    build: .
    container_name: ai-artifacts
    environment:
      - AGENT_NAME=ai-artifacts
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
