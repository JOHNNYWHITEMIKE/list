version: '3.8'

services:
  canary:
    build: .
    container_name: canary
    environment:
      - AGENT_NAME=canary
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
