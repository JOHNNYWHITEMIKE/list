version: '3.8'

services:
  ai_ethics_society:
    build: .
    container_name: ai_ethics_society
    environment:
      - AGENT_NAME=ai_ethics_society
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
