version: '3.8'

services:
  ai_apps:
    build: .
    container_name: ai_apps
    environment:
      - AGENT_NAME=ai_apps
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
