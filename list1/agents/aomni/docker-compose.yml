version: '3.8'

services:
  aomni:
    build: .
    container_name: aomni
    environment:
      - AGENT_NAME=aomni
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
