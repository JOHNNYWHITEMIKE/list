version: '3.8'

services:
  awesome-ai-llms-in-radiology:
    build: .
    container_name: awesome-ai-llms-in-radiology
    environment:
      - AGENT_NAME=awesome-ai-llms-in-radiology
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
