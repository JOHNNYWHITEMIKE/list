version: '3.8'

services:
  incredibledev:
    build: .
    container_name: incredibledev
    environment:
      - AGENT_NAME=incredibledev
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
