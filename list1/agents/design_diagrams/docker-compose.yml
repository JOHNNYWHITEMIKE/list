version: '3.8'

services:
  design_diagrams:
    build: .
    container_name: design_diagrams
    environment:
      - AGENT_NAME=design_diagrams
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
