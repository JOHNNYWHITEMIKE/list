version: '3.8'

services:
  awesome-llm-os:
    build: .
    container_name: awesome-llm-os
    environment:
      - AGENT_NAME=awesome-llm-os
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
