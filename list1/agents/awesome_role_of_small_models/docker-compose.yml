version: '3.8'

services:
  awesome_role_of_small_models:
    build: .
    container_name: awesome_role_of_small_models
    environment:
      - AGENT_NAME=awesome_role_of_small_models
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
