version: '3.8'

services:
  alfred-github-workflow:
    build: .
    container_name: alfred-github-workflow
    environment:
      - AGENT_NAME=alfred-github-workflow
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
