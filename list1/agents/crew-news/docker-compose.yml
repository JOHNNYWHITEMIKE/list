version: '3.8'

services:
  crew-news:
    build: .
    container_name: crew-news
    environment:
      - AGENT_NAME=crew-news
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
