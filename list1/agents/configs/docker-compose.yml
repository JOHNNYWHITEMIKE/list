version: '3.8'

services:
  configs:
    build: .
    container_name: configs
    environment:
      - AGENT_NAME=configs
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
