version: '3.8'

services:
  agentverse:
    build: .
    container_name: agentverse
    environment:
      - AGENT_NAME=agentverse
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
