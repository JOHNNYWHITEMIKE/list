version: '3.8'

services:
  idseq-workflows:
    build: .
    container_name: idseq-workflows
    environment:
      - AGENT_NAME=idseq-workflows
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
