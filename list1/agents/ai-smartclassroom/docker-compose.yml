version: '3.8'

services:
  ai-smartclassroom:
    build: .
    container_name: ai-smartclassroom
    environment:
      - AGENT_NAME=ai-smartclassroom
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
