version: '3.8'

services:
  bucovia:
    build: .
    container_name: bucovia
    environment:
      - AGENT_NAME=bucovia
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
