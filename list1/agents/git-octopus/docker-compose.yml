version: '3.8'

services:
  git-octopus:
    build: .
    container_name: git-octopus
    environment:
      - AGENT_NAME=git-octopus
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
