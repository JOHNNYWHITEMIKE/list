version: '3.8'

services:
  aivideochat:
    build: .
    container_name: aivideochat
    environment:
      - AGENT_NAME=aivideochat
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
