version: '3.8'

services:
  acte:
    build: .
    container_name: acte
    environment:
      - AGENT_NAME=acte
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
