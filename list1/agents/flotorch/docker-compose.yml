version: '3.8'

services:
  flotorch:
    build: .
    container_name: flotorch
    environment:
      - AGENT_NAME=flotorch
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
