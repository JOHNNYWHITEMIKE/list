version: '3.8'

services:
  awesome-llm4security:
    build: .
    container_name: awesome-llm4security
    environment:
      - AGENT_NAME=awesome-llm4security
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
