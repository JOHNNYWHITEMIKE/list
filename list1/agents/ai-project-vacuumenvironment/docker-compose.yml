version: '3.8'

services:
  ai-project-vacuumenvironment:
    build: .
    container_name: ai-project-vacuumenvironment
    environment:
      - AGENT_NAME=ai-project-vacuumenvironment
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
