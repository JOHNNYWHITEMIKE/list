version: '3.8'

services:
  gpt-newspaper-by-tavily:
    build: .
    container_name: gpt-newspaper-by-tavily
    environment:
      - AGENT_NAME=gpt-newspaper-by-tavily
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
