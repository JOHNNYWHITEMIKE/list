version: '3.8'

services:
  awesome-llm-compression:
    build: .
    container_name: awesome-llm-compression
    environment:
      - AGENT_NAME=awesome-llm-compression
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
