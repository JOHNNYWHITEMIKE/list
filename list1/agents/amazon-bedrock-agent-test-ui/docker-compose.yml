version: '3.8'

services:
  amazon-bedrock-agent-test-ui:
    build: .
    container_name: amazon-bedrock-agent-test-ui
    environment:
      - AGENT_NAME=amazon-bedrock-agent-test-ui
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
