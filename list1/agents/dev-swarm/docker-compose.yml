version: '3.8'

services:
  dev-swarm:
    build: .
    container_name: dev-swarm
    environment:
      - AGENT_NAME=dev-swarm
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
