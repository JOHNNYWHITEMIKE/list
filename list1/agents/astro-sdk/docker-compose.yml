version: '3.8'

services:
  astro-sdk:
    build: .
    container_name: astro-sdk
    environment:
      - AGENT_NAME=astro-sdk
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
