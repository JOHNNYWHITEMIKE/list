version: '3.8'

services:
  ai-ethics-panel:
    build: .
    container_name: ai-ethics-panel
    environment:
      - AGENT_NAME=ai-ethics-panel
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
