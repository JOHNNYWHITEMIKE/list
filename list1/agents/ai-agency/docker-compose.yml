version: '3.8'

services:
  ai-agency:
    build: .
    container_name: ai-agency
    environment:
      - AGENT_NAME=ai-agency
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
