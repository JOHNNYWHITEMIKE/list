version: '3.8'

services:
  airflow:
    build: .
    container_name: airflow
    environment:
      - AGENT_NAME=airflow
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
