version: '3.8'

services:
  agentictrust:
    build: .
    container_name: agentictrust
    environment:
      - AGENT_NAME=agentictrust
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
