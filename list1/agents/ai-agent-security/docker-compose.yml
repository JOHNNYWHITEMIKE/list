version: '3.8'

services:
  ai-agent-security:
    build: .
    container_name: ai-agent-security
    environment:
      - AGENT_NAME=ai-agent-security
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
