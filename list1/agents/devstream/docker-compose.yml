version: '3.8'

services:
  devstream:
    build: .
    container_name: devstream
    environment:
      - AGENT_NAME=devstream
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
