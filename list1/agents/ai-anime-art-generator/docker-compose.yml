version: '3.8'

services:
  ai-anime-art-generator:
    build: .
    container_name: ai-anime-art-generator
    environment:
      - AGENT_NAME=ai-anime-art-generator
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
