version: '3.8'

services:
  ai-agent-playground:
    build: .
    container_name: ai-agent-playground
    environment:
      - AGENT_NAME=ai-agent-playground
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
