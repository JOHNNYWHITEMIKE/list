version: '3.8'

services:
  embodied-agent-interfacegithubio:
    build: .
    container_name: embodied-agent-interfacegithubio
    environment:
      - AGENT_NAME=embodied-agent-interfacegithubio
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
