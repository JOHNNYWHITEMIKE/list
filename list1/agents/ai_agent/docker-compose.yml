version: '3.8'

services:
  ai_agent:
    build: .
    container_name: ai_agent
    environment:
      - AGENT_NAME=ai_agent
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
