version: '3.8'

services:
  fastchat:
    build: .
    container_name: fastchat
    environment:
      - AGENT_NAME=fastchat
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
