version: '3.8'

services:
  guardrails:
    build: .
    container_name: guardrails
    environment:
      - AGENT_NAME=guardrails
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
