version: '3.8'

services:
  ai_security_agent:
    build: .
    container_name: ai_security_agent
    environment:
      - AGENT_NAME=ai_security_agent
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
