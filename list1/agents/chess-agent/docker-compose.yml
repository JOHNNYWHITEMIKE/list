version: '3.8'

services:
  chess-agent:
    build: .
    container_name: chess-agent
    environment:
      - AGENT_NAME=chess-agent
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
