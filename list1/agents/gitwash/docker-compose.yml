version: '3.8'

services:
  gitwash:
    build: .
    container_name: gitwash
    environment:
      - AGENT_NAME=gitwash
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
