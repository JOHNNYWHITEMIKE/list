version: '3.8'

services:
  42dot_llm:
    build: .
    container_name: 42dot_llm
    environment:
      - AGENT_NAME=42dot_llm
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
