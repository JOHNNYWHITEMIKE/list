version: '3.8'

services:
  aws-templates:
    build: .
    container_name: aws-templates
    environment:
      - AGENT_NAME=aws-templates
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
