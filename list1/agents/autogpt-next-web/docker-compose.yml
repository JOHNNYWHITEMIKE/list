version: '3.8'

services:
  autogpt-next-web:
    build: .
    container_name: autogpt-next-web
    environment:
      - AGENT_NAME=autogpt-next-web
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
