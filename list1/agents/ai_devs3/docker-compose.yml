version: '3.8'

services:
  ai_devs3:
    build: .
    container_name: ai_devs3
    environment:
      - AGENT_NAME=ai_devs3
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
