version: '3.8'

services:
  crewai-examples:
    build: .
    container_name: crewai-examples
    environment:
      - AGENT_NAME=crewai-examples
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
