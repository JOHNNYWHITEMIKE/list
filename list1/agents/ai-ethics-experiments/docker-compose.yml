version: '3.8'

services:
  ai-ethics-experiments:
    build: .
    container_name: ai-ethics-experiments
    environment:
      - AGENT_NAME=ai-ethics-experiments
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
