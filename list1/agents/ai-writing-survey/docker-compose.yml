version: '3.8'

services:
  ai-writing-survey:
    build: .
    container_name: ai-writing-survey
    environment:
      - AGENT_NAME=ai-writing-survey
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
