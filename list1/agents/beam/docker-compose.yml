version: '3.8'

services:
  beam:
    build: .
    container_name: beam
    environment:
      - AGENT_NAME=beam
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
