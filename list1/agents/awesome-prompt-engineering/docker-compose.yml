version: '3.8'

services:
  awesome-prompt-engineering:
    build: .
    container_name: awesome-prompt-engineering
    environment:
      - AGENT_NAME=awesome-prompt-engineering
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
