version: '3.8'

services:
  iauto:
    build: .
    container_name: iauto
    environment:
      - AGENT_NAME=iauto
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
