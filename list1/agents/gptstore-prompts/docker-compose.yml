version: '3.8'

services:
  gptstore-prompts:
    build: .
    container_name: gptstore-prompts
    environment:
      - AGENT_NAME=gptstore-prompts
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
