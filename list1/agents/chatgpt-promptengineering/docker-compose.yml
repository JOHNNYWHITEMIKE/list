version: '3.8'

services:
  chatgpt-promptengineering:
    build: .
    container_name: chatgpt-promptengineering
    environment:
      - AGENT_NAME=chatgpt-promptengineering
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
