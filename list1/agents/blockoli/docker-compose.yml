version: '3.8'

services:
  blockoli:
    build: .
    container_name: blockoli
    environment:
      - AGENT_NAME=blockoli
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
