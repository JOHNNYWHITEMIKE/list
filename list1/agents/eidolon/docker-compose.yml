version: '3.8'

services:
  eidolon:
    build: .
    container_name: eidolon
    environment:
      - AGENT_NAME=eidolon
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
