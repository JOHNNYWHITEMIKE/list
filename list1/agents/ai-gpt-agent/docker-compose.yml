version: '3.8'

services:
  ai-gpt-agent:
    build: .
    container_name: ai-gpt-agent
    environment:
      - AGENT_NAME=ai-gpt-agent
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
