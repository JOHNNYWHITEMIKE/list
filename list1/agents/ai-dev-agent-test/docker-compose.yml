version: '3.8'

services:
  ai-dev-agent-test:
    build: .
    container_name: ai-dev-agent-test
    environment:
      - AGENT_NAME=ai-dev-agent-test
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
