version: '3.8'

services:
  codegen:
    build: .
    container_name: codegen
    environment:
      - AGENT_NAME=codegen
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
