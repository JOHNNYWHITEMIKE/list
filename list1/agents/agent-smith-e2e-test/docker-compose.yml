version: '3.8'

services:
  agent-smith-e2e-test:
    build: .
    container_name: agent-smith-e2e-test
    environment:
      - AGENT_NAME=agent-smith-e2e-test
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
