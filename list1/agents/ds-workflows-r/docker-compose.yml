version: '3.8'

services:
  ds-workflows-r:
    build: .
    container_name: ds-workflows-r
    environment:
      - AGENT_NAME=ds-workflows-r
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
