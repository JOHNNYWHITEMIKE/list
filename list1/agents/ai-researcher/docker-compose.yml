version: '3.8'

services:
  ai-researcher:
    build: .
    container_name: ai-researcher
    environment:
      - AGENT_NAME=ai-researcher
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
