version: '3.8'

services:
  conversational-agent-with-qa-tool:
    build: .
    container_name: conversational-agent-with-qa-tool
    environment:
      - AGENT_NAME=conversational-agent-with-qa-tool
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
