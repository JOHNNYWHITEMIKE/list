version: '3.8'

services:
  awesome-llm-json:
    build: .
    container_name: awesome-llm-json
    environment:
      - AGENT_NAME=awesome-llm-json
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
