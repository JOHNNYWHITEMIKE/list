version: '3.8'

services:
  flux-core:
    build: .
    container_name: flux-core
    environment:
      - AGENT_NAME=flux-core
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
