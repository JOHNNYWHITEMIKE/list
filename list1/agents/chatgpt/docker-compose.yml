version: '3.8'

services:
  chatgpt:
    build: .
    container_name: chatgpt
    environment:
      - AGENT_NAME=chatgpt
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
