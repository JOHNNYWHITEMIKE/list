version: '3.8'

services:
  github-actions:
    build: .
    container_name: github-actions
    environment:
      - AGENT_NAME=github-actions
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
