version: '3.8'

services:
  ai-agents-directory:
    build: .
    container_name: ai-agents-directory
    environment:
      - AGENT_NAME=ai-agents-directory
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
