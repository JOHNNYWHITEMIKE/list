version: '3.8'

services:
  graphql-cli:
    build: .
    container_name: graphql-cli
    environment:
      - AGENT_NAME=graphql-cli
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
