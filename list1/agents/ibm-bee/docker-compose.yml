version: '3.8'

services:
  ibm-bee:
    build: .
    container_name: ibm-bee
    environment:
      - AGENT_NAME=ibm-bee
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
