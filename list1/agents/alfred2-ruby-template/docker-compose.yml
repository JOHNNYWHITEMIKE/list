version: '3.8'

services:
  alfred2-ruby-template:
    build: .
    container_name: alfred2-ruby-template
    environment:
      - AGENT_NAME=alfred2-ruby-template
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
