version: '3.8'

services:
  agentos:
    build: .
    container_name: agentos
    environment:
      - AGENT_NAME=agentos
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
