version: '3.8'

services:
  flowbench:
    build: .
    container_name: flowbench
    environment:
      - AGENT_NAME=flowbench
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
