version: '3.8'

services:
  agentlabs:
    build: .
    container_name: agentlabs
    environment:
      - AGENT_NAME=agentlabs
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
