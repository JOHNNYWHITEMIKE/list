version: '3.8'

services:
  abc-classroom:
    build: .
    container_name: abc-classroom
    environment:
      - AGENT_NAME=abc-classroom
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
