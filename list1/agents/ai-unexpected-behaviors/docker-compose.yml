version: '3.8'

services:
  ai-unexpected-behaviors:
    build: .
    container_name: ai-unexpected-behaviors
    environment:
      - AGENT_NAME=ai-unexpected-behaviors
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
