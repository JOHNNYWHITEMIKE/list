version: '3.8'

services:
  chartgpt:
    build: .
    container_name: chartgpt
    environment:
      - AGENT_NAME=chartgpt
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
