version: '3.8'

services:
  awesome-prompts:
    build: .
    container_name: awesome-prompts
    environment:
      - AGENT_NAME=awesome-prompts
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
