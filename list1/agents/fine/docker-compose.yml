version: '3.8'

services:
  fine:
    build: .
    container_name: fine
    environment:
      - AGENT_NAME=fine
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
