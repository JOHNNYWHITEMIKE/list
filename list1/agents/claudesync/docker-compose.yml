version: '3.8'

services:
  claudesync:
    build: .
    container_name: claudesync
    environment:
      - AGENT_NAME=claudesync
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
