version: '3.8'

services:
  ai-api-testing:
    build: .
    container_name: ai-api-testing
    environment:
      - AGENT_NAME=ai-api-testing
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
