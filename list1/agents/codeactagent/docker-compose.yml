version: '3.8'

services:
  codeactagent:
    build: .
    container_name: codeactagent
    environment:
      - AGENT_NAME=codeactagent
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
