version: '3.8'

services:
  alfred-pinboard-rs:
    build: .
    container_name: alfred-pinboard-rs
    environment:
      - AGENT_NAME=alfred-pinboard-rs
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
