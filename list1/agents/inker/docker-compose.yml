version: '3.8'

services:
  inker:
    build: .
    container_name: inker
    environment:
      - AGENT_NAME=inker
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
