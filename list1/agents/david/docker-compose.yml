version: '3.8'

services:
  david:
    build: .
    container_name: david
    environment:
      - AGENT_NAME=david
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
