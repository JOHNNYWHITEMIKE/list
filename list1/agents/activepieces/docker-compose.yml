version: '3.8'

services:
  activepieces:
    build: .
    container_name: activepieces
    environment:
      - AGENT_NAME=activepieces
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
