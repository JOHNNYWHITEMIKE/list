version: '3.8'

services:
  harpa:
    build: .
    container_name: harpa
    environment:
      - AGENT_NAME=harpa
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
