version: '3.8'

services:
  acm:
    build: .
    container_name: acm
    environment:
      - AGENT_NAME=acm
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
