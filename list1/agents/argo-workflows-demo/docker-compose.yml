version: '3.8'

services:
  argo-workflows-demo:
    build: .
    container_name: argo-workflows-demo
    environment:
      - AGENT_NAME=argo-workflows-demo
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
