version: '3.8'

services:
  dellma:
    build: .
    container_name: dellma
    environment:
      - AGENT_NAME=dellma
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
