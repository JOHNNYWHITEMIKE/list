version: '3.8'

services:
  clin:
    build: .
    container_name: clin
    environment:
      - AGENT_NAME=clin
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
