version: '3.8'

services:
  corewf:
    build: .
    container_name: corewf
    environment:
      - AGENT_NAME=corewf
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
