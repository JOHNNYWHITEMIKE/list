version: '3.8'

services:
  agent-evaluation:
    build: .
    container_name: agent-evaluation
    environment:
      - AGENT_NAME=agent-evaluation
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
