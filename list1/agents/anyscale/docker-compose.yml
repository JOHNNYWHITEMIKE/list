version: '3.8'

services:
  anyscale:
    build: .
    container_name: anyscale
    environment:
      - AGENT_NAME=anyscale
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
