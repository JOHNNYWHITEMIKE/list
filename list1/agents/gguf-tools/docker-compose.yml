version: '3.8'

services:
  gguf-tools:
    build: .
    container_name: gguf-tools
    environment:
      - AGENT_NAME=gguf-tools
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
