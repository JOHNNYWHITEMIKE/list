version: '3.8'

services:
  ethicai:
    build: .
    container_name: ethicai
    environment:
      - AGENT_NAME=ethicai
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
