version: '3.8'

services:
  blueprints:
    build: .
    container_name: blueprints
    environment:
      - AGENT_NAME=blueprints
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
