version: '3.8'

services:
  git-guppy:
    build: .
    container_name: git-guppy
    environment:
      - AGENT_NAME=git-guppy
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
