version: '3.8'

services:
  ainote:
    build: .
    container_name: ainote
    environment:
      - AGENT_NAME=ainote
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
