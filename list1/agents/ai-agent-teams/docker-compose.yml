version: '3.8'

services:
  ai-agent-teams:
    build: .
    container_name: ai-agent-teams
    environment:
      - AGENT_NAME=ai-agent-teams
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
