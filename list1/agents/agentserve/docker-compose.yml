version: '3.8'

services:
  agentserve:
    build: .
    container_name: agentserve
    environment:
      - AGENT_NAME=agentserve
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
