version: '3.8'

services:
  dataset-translator:
    build: .
    container_name: dataset-translator
    environment:
      - AGENT_NAME=dataset-translator
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
