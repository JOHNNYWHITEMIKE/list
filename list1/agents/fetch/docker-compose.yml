version: '3.8'

services:
  fetch:
    build: .
    container_name: fetch
    environment:
      - AGENT_NAME=fetch
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
