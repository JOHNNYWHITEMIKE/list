version: '3.8'

services:
  anthropic:
    build: .
    container_name: anthropic
    environment:
      - AGENT_NAME=anthropic
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
