version: '3.8'

services:
  diffsensei:
    build: .
    container_name: diffsensei
    environment:
      - AGENT_NAME=diffsensei
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
