version: '3.8'

services:
  goodai-ltm-benchmark:
    build: .
    container_name: goodai-ltm-benchmark
    environment:
      - AGENT_NAME=goodai-ltm-benchmark
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
