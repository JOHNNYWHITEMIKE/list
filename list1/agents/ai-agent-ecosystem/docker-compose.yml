version: '3.8'

services:
  ai-agent-ecosystem:
    build: .
    container_name: ai-agent-ecosystem
    environment:
      - AGENT_NAME=ai-agent-ecosystem
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
