version: '3.8'

services:
  ethics-in-ai:
    build: .
    container_name: ethics-in-ai
    environment:
      - AGENT_NAME=ethics-in-ai
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
