version: '3.8'

services:
  agent-protocol:
    build: .
    container_name: agent-protocol
    environment:
      - AGENT_NAME=agent-protocol
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
