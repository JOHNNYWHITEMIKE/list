version: '3.8'

services:
  awesome-recommend-system-pretraining-papers:
    build: .
    container_name: awesome-recommend-system-pretraining-papers
    environment:
      - AGENT_NAME=awesome-recommend-system-pretraining-papers
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
