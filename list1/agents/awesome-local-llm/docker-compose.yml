version: '3.8'

services:
  awesome-local-llm:
    build: .
    container_name: awesome-local-llm
    environment:
      - AGENT_NAME=awesome-local-llm
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
