version: '3.8'

services:
  imbue:
    build: .
    container_name: imbue
    environment:
      - AGENT_NAME=imbue
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
