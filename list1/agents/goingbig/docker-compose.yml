version: '3.8'

services:
  goingbig:
    build: .
    container_name: goingbig
    environment:
      - AGENT_NAME=goingbig
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
