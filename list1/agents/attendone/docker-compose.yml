version: '3.8'

services:
  attendone:
    build: .
    container_name: attendone
    environment:
      - AGENT_NAME=attendone
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
