version: '3.8'

services:
  ai_ethics:
    build: .
    container_name: ai_ethics
    environment:
      - AGENT_NAME=ai_ethics
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
