version: '3.8'

services:
  axocore:
    build: .
    container_name: axocore
    environment:
      - AGENT_NAME=axocore
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
