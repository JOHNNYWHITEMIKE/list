version: '3.8'

services:
  gpt4-programming-assistant:
    build: .
    container_name: gpt4-programming-assistant
    environment:
      - AGENT_NAME=gpt4-programming-assistant
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
