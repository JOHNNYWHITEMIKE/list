version: '3.8'

services:
  ai-sdk-by-vercel:
    build: .
    container_name: ai-sdk-by-vercel
    environment:
      - AGENT_NAME=ai-sdk-by-vercel
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
