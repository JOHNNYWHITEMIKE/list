version: '3.8'

services:
  ai_ethics_bookclub:
    build: .
    container_name: ai_ethics_bookclub
    environment:
      - AGENT_NAME=ai_ethics_bookclub
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
