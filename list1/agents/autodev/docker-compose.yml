version: '3.8'

services:
  autodev:
    build: .
    container_name: autodev
    environment:
      - AGENT_NAME=autodev
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
