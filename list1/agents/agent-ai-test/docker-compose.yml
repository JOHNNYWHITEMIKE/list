version: '3.8'

services:
  agent-ai-test:
    build: .
    container_name: agent-ai-test
    environment:
      - AGENT_NAME=agent-ai-test
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
