version: '3.8'

services:
  ailoveragent:
    build: .
    container_name: ailoveragent
    environment:
      - AGENT_NAME=ailoveragent
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
