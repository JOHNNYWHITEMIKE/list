version: '3.8'

services:
  ethiquest-ai-dilemma-game:
    build: .
    container_name: ethiquest-ai-dilemma-game
    environment:
      - AGENT_NAME=ethiquest-ai-dilemma-game
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
