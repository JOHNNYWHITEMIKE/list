version: '3.8'

services:
  devika:
    build: .
    container_name: devika
    environment:
      - AGENT_NAME=devika
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
