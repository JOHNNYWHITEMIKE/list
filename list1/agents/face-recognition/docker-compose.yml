version: '3.8'

services:
  face-recognition:
    build: .
    container_name: face-recognition
    environment:
      - AGENT_NAME=face-recognition
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
