version: '3.8'

services:
  ai-powered-security-agent:
    build: .
    container_name: ai-powered-security-agent
    environment:
      - AGENT_NAME=ai-powered-security-agent
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
