version: '3.8'

services:
  devopsgpt:
    build: .
    container_name: devopsgpt
    environment:
      - AGENT_NAME=devopsgpt
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
