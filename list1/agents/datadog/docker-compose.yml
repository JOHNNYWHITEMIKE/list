version: '3.8'

services:
  datadog:
    build: .
    container_name: datadog
    environment:
      - AGENT_NAME=datadog
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
