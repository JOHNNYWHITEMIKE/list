version: '3.8'

services:
  anthropic-agent-in-docker:
    build: .
    container_name: anthropic-agent-in-docker
    environment:
      - AGENT_NAME=anthropic-agent-in-docker
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
