version: '3.8'

services:
  heb-gen-ai:
    build: .
    container_name: heb-gen-ai
    environment:
      - AGENT_NAME=heb-gen-ai
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
