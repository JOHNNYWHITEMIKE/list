version: '3.8'

services:
  agentic-ai:
    build: .
    container_name: agentic-ai
    environment:
      - AGENT_NAME=agentic-ai
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
