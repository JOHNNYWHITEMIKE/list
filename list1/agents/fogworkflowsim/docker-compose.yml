version: '3.8'

services:
  fogworkflowsim:
    build: .
    container_name: fogworkflowsim
    environment:
      - AGENT_NAME=fogworkflowsim
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
