version: '3.8'

services:
  chatbot:
    build: .
    container_name: chatbot
    environment:
      - AGENT_NAME=chatbot
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
