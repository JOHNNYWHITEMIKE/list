version: '3.8'

services:
  book-dataset:
    build: .
    container_name: book-dataset
    environment:
      - AGENT_NAME=book-dataset
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
