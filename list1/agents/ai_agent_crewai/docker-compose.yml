version: '3.8'

services:
  ai_agent_crewai:
    build: .
    container_name: ai_agent_crewai
    environment:
      - AGENT_NAME=ai_agent_crewai
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
