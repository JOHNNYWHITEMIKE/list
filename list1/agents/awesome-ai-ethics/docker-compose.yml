version: '3.8'

services:
  awesome-ai-ethics:
    build: .
    container_name: awesome-ai-ethics
    environment:
      - AGENT_NAME=awesome-ai-ethics
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
