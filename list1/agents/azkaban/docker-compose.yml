version: '3.8'

services:
  azkaban:
    build: .
    container_name: azkaban
    environment:
      - AGENT_NAME=azkaban
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
