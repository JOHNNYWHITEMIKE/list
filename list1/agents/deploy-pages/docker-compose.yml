version: '3.8'

services:
  deploy-pages:
    build: .
    container_name: deploy-pages
    environment:
      - AGENT_NAME=deploy-pages
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
