version: '3.8'

services:
  aiagents_test:
    build: .
    container_name: aiagents_test
    environment:
      - AGENT_NAME=aiagents_test
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
