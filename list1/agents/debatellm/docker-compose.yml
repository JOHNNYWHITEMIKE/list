version: '3.8'

services:
  debatellm:
    build: .
    container_name: debatellm
    environment:
      - AGENT_NAME=debatellm
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
