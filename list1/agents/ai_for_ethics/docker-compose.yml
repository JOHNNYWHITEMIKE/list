version: '3.8'

services:
  ai_for_ethics:
    build: .
    container_name: ai_for_ethics
    environment:
      - AGENT_NAME=ai_for_ethics
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
