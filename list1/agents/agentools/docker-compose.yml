version: '3.8'

services:
  agentools:
    build: .
    container_name: agentools
    environment:
      - AGENT_NAME=agentools
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
