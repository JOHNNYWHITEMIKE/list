version: '3.8'

services:
  bland:
    build: .
    container_name: bland
    environment:
      - AGENT_NAME=bland
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
