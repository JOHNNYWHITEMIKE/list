version: '3.8'

services:
  basaran:
    build: .
    container_name: basaran
    environment:
      - AGENT_NAME=basaran
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
