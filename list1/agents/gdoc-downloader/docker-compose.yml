version: '3.8'

services:
  gdoc-downloader:
    build: .
    container_name: gdoc-downloader
    environment:
      - AGENT_NAME=gdoc-downloader
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
