version: '3.8'

services:
  ai_buddy_guard:
    build: .
    container_name: ai_buddy_guard
    environment:
      - AGENT_NAME=ai_buddy_guard
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
