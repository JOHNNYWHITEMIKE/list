version: '3.8'

services:
  fake_news_detection:
    build: .
    container_name: fake_news_detection
    environment:
      - AGENT_NAME=fake_news_detection
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
