version: '3.8'

services:
  freeze-omni:
    build: .
    container_name: freeze-omni
    environment:
      - AGENT_NAME=freeze-omni
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
