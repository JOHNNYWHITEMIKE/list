version: '3.8'

services:
  agents:
    build: .
    container_name: agents
    environment:
      - AGENT_NAME=agents
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
