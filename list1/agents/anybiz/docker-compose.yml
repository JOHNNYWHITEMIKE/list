version: '3.8'

services:
  anybiz:
    build: .
    container_name: anybiz
    environment:
      - AGENT_NAME=anybiz
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
