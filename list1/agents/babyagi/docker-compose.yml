version: '3.8'

services:
  babyagi:
    build: .
    container_name: babyagi
    environment:
      - AGENT_NAME=babyagi
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
