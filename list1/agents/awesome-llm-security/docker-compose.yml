version: '3.8'

services:
  awesome-llm-security:
    build: .
    container_name: awesome-llm-security
    environment:
      - AGENT_NAME=awesome-llm-security
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
