version: '3.8'

services:
  ai_gdpr:
    build: .
    container_name: ai_gdpr
    environment:
      - AGENT_NAME=ai_gdpr
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
