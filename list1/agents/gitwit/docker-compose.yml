version: '3.8'

services:
  gitwit:
    build: .
    container_name: gitwit
    environment:
      - AGENT_NAME=gitwit
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
