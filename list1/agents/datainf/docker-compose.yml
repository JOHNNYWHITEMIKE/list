version: '3.8'

services:
  datainf:
    build: .
    container_name: datainf
    environment:
      - AGENT_NAME=datainf
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
