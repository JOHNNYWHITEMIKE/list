version: '3.8'

services:
  ai-ethics:
    build: .
    container_name: ai-ethics
    environment:
      - AGENT_NAME=ai-ethics
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
