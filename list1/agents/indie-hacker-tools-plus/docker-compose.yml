version: '3.8'

services:
  indie-hacker-tools-plus:
    build: .
    container_name: indie-hacker-tools-plus
    environment:
      - AGENT_NAME=indie-hacker-tools-plus
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
