version: '3.8'

services:
  ai-ethics-framework:
    build: .
    container_name: ai-ethics-framework
    environment:
      - AGENT_NAME=ai-ethics-framework
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
