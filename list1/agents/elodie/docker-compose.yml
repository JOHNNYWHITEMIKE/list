version: '3.8'

services:
  elodie:
    build: .
    container_name: elodie
    environment:
      - AGENT_NAME=elodie
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
