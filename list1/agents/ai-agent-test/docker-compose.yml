version: '3.8'

services:
  ai-agent-test:
    build: .
    container_name: ai-agent-test
    environment:
      - AGENT_NAME=ai-agent-test
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
