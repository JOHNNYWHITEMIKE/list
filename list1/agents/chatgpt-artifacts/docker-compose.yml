version: '3.8'

services:
  chatgpt-artifacts:
    build: .
    container_name: chatgpt-artifacts
    environment:
      - AGENT_NAME=chatgpt-artifacts
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
