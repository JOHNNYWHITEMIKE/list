version: '3.8'

services:
  aws-ddk:
    build: .
    container_name: aws-ddk
    environment:
      - AGENT_NAME=aws-ddk
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
