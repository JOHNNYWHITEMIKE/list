version: '3.8'

services:
  atat:
    build: .
    container_name: atat
    environment:
      - AGENT_NAME=atat
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
