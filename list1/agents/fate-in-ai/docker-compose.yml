version: '3.8'

services:
  fate-in-ai:
    build: .
    container_name: fate-in-ai
    environment:
      - AGENT_NAME=fate-in-ai
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
