version: '3.8'

services:
  graph-instruction-tuning:
    build: .
    container_name: graph-instruction-tuning
    environment:
      - AGENT_NAME=graph-instruction-tuning
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
