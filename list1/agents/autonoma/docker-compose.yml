version: '3.8'

services:
  autonoma:
    build: .
    container_name: autonoma
    environment:
      - AGENT_NAME=autonoma
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
