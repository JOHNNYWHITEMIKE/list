version: '3.8'

services:
  ai-agent:
    build: .
    container_name: ai-agent
    environment:
      - AGENT_NAME=ai-agent
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
