version: '3.8'

services:
  crewai-101:
    build: .
    container_name: crewai-101
    environment:
      - AGENT_NAME=crewai-101
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
