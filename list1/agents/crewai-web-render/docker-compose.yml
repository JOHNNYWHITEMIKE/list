version: '3.8'

services:
  crewai-web-render:
    build: .
    container_name: crewai-web-render
    environment:
      - AGENT_NAME=crewai-web-render
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
