version: '3.8'

services:
  ai-ethics-talk:
    build: .
    container_name: ai-ethics-talk
    environment:
      - AGENT_NAME=ai-ethics-talk
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
