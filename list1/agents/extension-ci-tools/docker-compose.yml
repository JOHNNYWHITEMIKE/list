version: '3.8'

services:
  extension-ci-tools:
    build: .
    container_name: extension-ci-tools
    environment:
      - AGENT_NAME=extension-ci-tools
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
