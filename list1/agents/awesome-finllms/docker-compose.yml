version: '3.8'

services:
  awesome-finllms:
    build: .
    container_name: awesome-finllms
    environment:
      - AGENT_NAME=awesome-finllms
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
