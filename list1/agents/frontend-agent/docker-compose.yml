version: '3.8'

services:
  frontend-agent:
    build: .
    container_name: frontend-agent
    environment:
      - AGENT_NAME=frontend-agent
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
