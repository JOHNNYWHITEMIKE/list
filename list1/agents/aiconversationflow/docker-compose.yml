version: '3.8'

services:
  aiconversationflow:
    build: .
    container_name: aiconversationflow
    environment:
      - AGENT_NAME=aiconversationflow
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
