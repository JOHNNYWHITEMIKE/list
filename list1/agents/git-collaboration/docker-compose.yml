version: '3.8'

services:
  git-collaboration:
    build: .
    container_name: git-collaboration
    environment:
      - AGENT_NAME=git-collaboration
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
