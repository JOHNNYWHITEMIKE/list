version: '3.8'

services:
  cartlis:
    build: .
    container_name: cartlis
    environment:
      - AGENT_NAME=cartlis
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
