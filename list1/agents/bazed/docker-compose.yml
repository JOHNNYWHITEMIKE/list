version: '3.8'

services:
  bazed:
    build: .
    container_name: bazed
    environment:
      - AGENT_NAME=bazed
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
