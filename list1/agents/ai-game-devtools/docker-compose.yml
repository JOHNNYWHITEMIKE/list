version: '3.8'

services:
  ai-game-devtools:
    build: .
    container_name: ai-game-devtools
    environment:
      - AGENT_NAME=ai-game-devtools
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
