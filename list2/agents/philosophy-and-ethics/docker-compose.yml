version: '3.8'

services:
  philosophy-and-ethics:
    build: .
    container_name: philosophy-and-ethics
    environment:
      - AGENT_NAME=philosophy-and-ethics
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
