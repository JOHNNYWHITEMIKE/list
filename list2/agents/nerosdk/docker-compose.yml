version: '3.8'

services:
  nerosdk:
    build: .
    container_name: nerosdk
    environment:
      - AGENT_NAME=nerosdk
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
