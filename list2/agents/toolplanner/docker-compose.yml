version: '3.8'

services:
  toolplanner:
    build: .
    container_name: toolplanner
    environment:
      - AGENT_NAME=toolplanner
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
