version: '3.8'

services:
  libraria:
    build: .
    container_name: libraria
    environment:
      - AGENT_NAME=libraria
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
