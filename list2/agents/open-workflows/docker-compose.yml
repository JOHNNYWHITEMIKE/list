version: '3.8'

services:
  open-workflows:
    build: .
    container_name: open-workflows
    environment:
      - AGENT_NAME=open-workflows
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
