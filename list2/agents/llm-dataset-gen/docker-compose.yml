version: '3.8'

services:
  llm-dataset-gen:
    build: .
    container_name: llm-dataset-gen
    environment:
      - AGENT_NAME=llm-dataset-gen
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
