version: '3.8'

services:
  llm:
    build: .
    container_name: llm
    environment:
      - AGENT_NAME=llm
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
