version: '3.8'

services:
  luigi:
    build: .
    container_name: luigi
    environment:
      - AGENT_NAME=luigi
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
