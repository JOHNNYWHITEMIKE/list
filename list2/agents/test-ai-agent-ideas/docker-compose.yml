version: '3.8'

services:
  test-ai-agent-ideas:
    build: .
    container_name: test-ai-agent-ideas
    environment:
      - AGENT_NAME=test-ai-agent-ideas
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
