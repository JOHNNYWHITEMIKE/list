version: '3.8'

services:
  langchain-tools:
    build: .
    container_name: langchain-tools
    environment:
      - AGENT_NAME=langchain-tools
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
