version: '3.8'

services:
  sim-court:
    build: .
    container_name: sim-court
    environment:
      - AGENT_NAME=sim-court
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
