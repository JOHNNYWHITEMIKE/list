version: '3.8'

services:
  rgs:
    build: .
    container_name: rgs
    environment:
      - AGENT_NAME=rgs
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
