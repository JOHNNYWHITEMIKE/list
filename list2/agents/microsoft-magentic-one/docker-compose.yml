version: '3.8'

services:
  microsoft-magentic-one:
    build: .
    container_name: microsoft-magentic-one
    environment:
      - AGENT_NAME=microsoft-magentic-one
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
