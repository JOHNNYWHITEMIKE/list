version: '3.8'

services:
  worldai:
    build: .
    container_name: worldai
    environment:
      - AGENT_NAME=worldai
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
