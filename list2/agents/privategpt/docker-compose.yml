version: '3.8'

services:
  privategpt:
    build: .
    container_name: privategpt
    environment:
      - AGENT_NAME=privategpt
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
