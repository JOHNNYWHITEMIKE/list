version: '3.8'

services:
  sg-data-analyst:
    build: .
    container_name: sg-data-analyst
    environment:
      - AGENT_NAME=sg-data-analyst
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
