version: '3.8'

services:
  realmofrespect:
    build: .
    container_name: realmofrespect
    environment:
      - AGENT_NAME=realmofrespect
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
