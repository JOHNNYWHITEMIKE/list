version: '3.8'

services:
  session-20-ethics-in-data-science:
    build: .
    container_name: session-20-ethics-in-data-science
    environment:
      - AGENT_NAME=session-20-ethics-in-data-science
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
