version: '3.8'

services:
  llm-interest:
    build: .
    container_name: llm-interest
    environment:
      - AGENT_NAME=llm-interest
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
