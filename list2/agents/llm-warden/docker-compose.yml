version: '3.8'

services:
  llm-warden:
    build: .
    container_name: llm-warden
    environment:
      - AGENT_NAME=llm-warden
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
