version: '3.8'

services:
  voice_agent:
    build: .
    container_name: voice_agent
    environment:
      - AGENT_NAME=voice_agent
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
