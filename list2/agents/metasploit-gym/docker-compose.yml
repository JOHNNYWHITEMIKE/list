version: '3.8'

services:
  metasploit-gym:
    build: .
    container_name: metasploit-gym
    environment:
      - AGENT_NAME=metasploit-gym
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
