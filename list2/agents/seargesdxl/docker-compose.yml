version: '3.8'

services:
  seargesdxl:
    build: .
    container_name: seargesdxl
    environment:
      - AGENT_NAME=seargesdxl
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
