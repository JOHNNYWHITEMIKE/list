version: '3.8'

services:
  justben8:
    build: .
    container_name: justben8
    environment:
      - AGENT_NAME=justben8
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
