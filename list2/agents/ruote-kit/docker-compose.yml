version: '3.8'

services:
  ruote-kit:
    build: .
    container_name: ruote-kit
    environment:
      - AGENT_NAME=ruote-kit
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
