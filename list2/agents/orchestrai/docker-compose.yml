version: '3.8'

services:
  orchestrai:
    build: .
    container_name: orchestrai
    environment:
      - AGENT_NAME=orchestrai
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
