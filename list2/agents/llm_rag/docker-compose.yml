version: '3.8'

services:
  llm_rag:
    build: .
    container_name: llm_rag
    environment:
      - AGENT_NAME=llm_rag
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
