version: '3.8'

services:
  rag_llm-on-customdataset:
    build: .
    container_name: rag_llm-on-customdataset
    environment:
      - AGENT_NAME=rag_llm-on-customdataset
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
