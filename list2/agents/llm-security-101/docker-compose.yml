version: '3.8'

services:
  llm-security-101:
    build: .
    container_name: llm-security-101
    environment:
      - AGENT_NAME=llm-security-101
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
