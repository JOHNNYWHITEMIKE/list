version: '3.8'

services:
  microsoft-semantic-kernel:
    build: .
    container_name: microsoft-semantic-kernel
    environment:
      - AGENT_NAME=microsoft-semantic-kernel
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
