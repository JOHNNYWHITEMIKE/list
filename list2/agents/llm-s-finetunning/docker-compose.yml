version: '3.8'

services:
  llm-s-finetunning:
    build: .
    container_name: llm-s-finetunning
    environment:
      - AGENT_NAME=llm-s-finetunning
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
