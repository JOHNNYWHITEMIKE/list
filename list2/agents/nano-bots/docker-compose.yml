version: '3.8'

services:
  nano-bots:
    build: .
    container_name: nano-bots
    environment:
      - AGENT_NAME=nano-bots
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
