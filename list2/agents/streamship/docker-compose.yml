version: '3.8'

services:
  streamship:
    build: .
    container_name: streamship
    environment:
      - AGENT_NAME=streamship
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
