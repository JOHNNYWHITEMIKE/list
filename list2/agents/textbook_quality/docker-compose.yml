version: '3.8'

services:
  textbook_quality:
    build: .
    container_name: textbook_quality
    environment:
      - AGENT_NAME=textbook_quality
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
