version: '3.8'

services:
  sandbox:
    build: .
    container_name: sandbox
    environment:
      - AGENT_NAME=sandbox
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
