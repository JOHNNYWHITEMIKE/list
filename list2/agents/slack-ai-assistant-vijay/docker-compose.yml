version: '3.8'

services:
  slack-ai-assistant-vijay:
    build: .
    container_name: slack-ai-assistant-vijay
    environment:
      - AGENT_NAME=slack-ai-assistant-vijay
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
