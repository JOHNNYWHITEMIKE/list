version: '3.8'

services:
  nest-ai:
    build: .
    container_name: nest-ai
    environment:
      - AGENT_NAME=nest-ai
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
