version: '3.8'

services:
  swarmsxgcp:
    build: .
    container_name: swarmsxgcp
    environment:
      - AGENT_NAME=swarmsxgcp
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
