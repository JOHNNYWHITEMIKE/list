version: '3.8'

services:
  syntune:
    build: .
    container_name: syntune
    environment:
      - AGENT_NAME=syntune
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
