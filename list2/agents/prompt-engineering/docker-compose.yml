version: '3.8'

services:
  prompt-engineering:
    build: .
    container_name: prompt-engineering
    environment:
      - AGENT_NAME=prompt-engineering
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
