version: '3.8'

services:
  mql:
    build: .
    container_name: mql
    environment:
      - AGENT_NAME=mql
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
