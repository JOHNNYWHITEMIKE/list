version: '3.8'

services:
  marvin:
    build: .
    container_name: marvin
    environment:
      - AGENT_NAME=marvin
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
