version: '3.8'

services:
  sam:
    build: .
    container_name: sam
    environment:
      - AGENT_NAME=sam
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
