version: '3.8'

services:
  litellm:
    build: .
    container_name: litellm
    environment:
      - AGENT_NAME=litellm
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
