version: '3.8'

services:
  spreads:
    build: .
    container_name: spreads
    environment:
      - AGENT_NAME=spreads
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
