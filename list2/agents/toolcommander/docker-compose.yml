version: '3.8'

services:
  toolcommander:
    build: .
    container_name: toolcommander
    environment:
      - AGENT_NAME=toolcommander
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
