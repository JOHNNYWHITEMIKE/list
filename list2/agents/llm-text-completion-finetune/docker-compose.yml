version: '3.8'

services:
  llm-text-completion-finetune:
    build: .
    container_name: llm-text-completion-finetune
    environment:
      - AGENT_NAME=llm-text-completion-finetune
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
