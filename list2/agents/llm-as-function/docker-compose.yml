version: '3.8'

services:
  llm-as-function:
    build: .
    container_name: llm-as-function
    environment:
      - AGENT_NAME=llm-as-function
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
