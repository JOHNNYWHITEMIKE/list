version: '3.8'

services:
  llmsmartaudittool:
    build: .
    container_name: llmsmartaudittool
    environment:
      - AGENT_NAME=llmsmartaudittool
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
