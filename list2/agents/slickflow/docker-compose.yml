version: '3.8'

services:
  slickflow:
    build: .
    container_name: slickflow
    environment:
      - AGENT_NAME=slickflow
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
