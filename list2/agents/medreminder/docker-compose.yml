version: '3.8'

services:
  medreminder:
    build: .
    container_name: medreminder
    environment:
      - AGENT_NAME=medreminder
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
