version: '3.8'

services:
  thoughtloom:
    build: .
    container_name: thoughtloom
    environment:
      - AGENT_NAME=thoughtloom
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
