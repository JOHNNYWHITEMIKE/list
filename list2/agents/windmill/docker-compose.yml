version: '3.8'

services:
  windmill:
    build: .
    container_name: windmill
    environment:
      - AGENT_NAME=windmill
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
