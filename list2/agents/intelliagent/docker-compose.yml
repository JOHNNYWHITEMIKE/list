version: '3.8'

services:
  intelliagent:
    build: .
    container_name: intelliagent
    environment:
      - AGENT_NAME=intelliagent
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
