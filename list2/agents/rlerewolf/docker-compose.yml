version: '3.8'

services:
  rlerewolf:
    build: .
    container_name: rlerewolf
    environment:
      - AGENT_NAME=rlerewolf
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
