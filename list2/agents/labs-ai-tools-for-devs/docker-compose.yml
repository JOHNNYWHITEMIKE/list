version: '3.8'

services:
  labs-ai-tools-for-devs:
    build: .
    container_name: labs-ai-tools-for-devs
    environment:
      - AGENT_NAME=labs-ai-tools-for-devs
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
