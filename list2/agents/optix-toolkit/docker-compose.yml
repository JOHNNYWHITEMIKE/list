version: '3.8'

services:
  optix-toolkit:
    build: .
    container_name: optix-toolkit
    environment:
      - AGENT_NAME=optix-toolkit
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
