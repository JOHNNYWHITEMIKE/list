version: '3.8'

services:
  llm_scavengerhunt:
    build: .
    container_name: llm_scavengerhunt
    environment:
      - AGENT_NAME=llm_scavengerhunt
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
