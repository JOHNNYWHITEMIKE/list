version: '3.8'

services:
  yival:
    build: .
    container_name: yival
    environment:
      - AGENT_NAME=yival
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
