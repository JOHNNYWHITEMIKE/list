version: '3.8'

services:
  npi:
    build: .
    container_name: npi
    environment:
      - AGENT_NAME=npi
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
