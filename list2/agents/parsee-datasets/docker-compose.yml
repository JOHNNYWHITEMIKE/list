version: '3.8'

services:
  parsee-datasets:
    build: .
    container_name: parsee-datasets
    environment:
      - AGENT_NAME=parsee-datasets
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
