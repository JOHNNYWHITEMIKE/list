version: '3.8'

services:
  repo-to-text:
    build: .
    container_name: repo-to-text
    environment:
      - AGENT_NAME=repo-to-text
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
