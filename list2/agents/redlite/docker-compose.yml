version: '3.8'

services:
  redlite:
    build: .
    container_name: redlite
    environment:
      - AGENT_NAME=redlite
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
