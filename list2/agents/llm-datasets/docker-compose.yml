version: '3.8'

services:
  llm-datasets:
    build: .
    container_name: llm-datasets
    environment:
      - AGENT_NAME=llm-datasets
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
