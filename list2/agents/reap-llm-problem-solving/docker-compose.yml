version: '3.8'

services:
  reap-llm-problem-solving:
    build: .
    container_name: reap-llm-problem-solving
    environment:
      - AGENT_NAME=reap-llm-problem-solving
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
