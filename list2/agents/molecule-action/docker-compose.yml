version: '3.8'

services:
  molecule-action:
    build: .
    container_name: molecule-action
    environment:
      - AGENT_NAME=molecule-action
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
