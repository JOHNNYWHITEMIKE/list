version: '3.8'

services:
  perl-workflow:
    build: .
    container_name: perl-workflow
    environment:
      - AGENT_NAME=perl-workflow
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
