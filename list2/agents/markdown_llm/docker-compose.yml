version: '3.8'

services:
  markdown_llm:
    build: .
    container_name: markdown_llm
    environment:
      - AGENT_NAME=markdown_llm
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
