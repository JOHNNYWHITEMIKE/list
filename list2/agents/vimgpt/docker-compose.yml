version: '3.8'

services:
  vimgpt:
    build: .
    container_name: vimgpt
    environment:
      - AGENT_NAME=vimgpt
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
