version: '3.8'

services:
  prelude:
    build: .
    container_name: prelude
    environment:
      - AGENT_NAME=prelude
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
