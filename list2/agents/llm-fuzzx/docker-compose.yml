version: '3.8'

services:
  llm-fuzzx:
    build: .
    container_name: llm-fuzzx
    environment:
      - AGENT_NAME=llm-fuzzx
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
