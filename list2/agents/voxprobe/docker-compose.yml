version: '3.8'

services:
  voxprobe:
    build: .
    container_name: voxprobe
    environment:
      - AGENT_NAME=voxprobe
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
