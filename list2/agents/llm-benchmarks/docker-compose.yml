version: '3.8'

services:
  llm-benchmarks:
    build: .
    container_name: llm-benchmarks
    environment:
      - AGENT_NAME=llm-benchmarks
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
