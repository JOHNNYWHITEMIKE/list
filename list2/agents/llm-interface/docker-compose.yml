version: '3.8'

services:
  llm-interface:
    build: .
    container_name: llm-interface
    environment:
      - AGENT_NAME=llm-interface
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
