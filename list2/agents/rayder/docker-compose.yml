version: '3.8'

services:
  rayder:
    build: .
    container_name: rayder
    environment:
      - AGENT_NAME=rayder
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
