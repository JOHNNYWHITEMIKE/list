version: '3.8'

services:
  vlm_databuilder:
    build: .
    container_name: vlm_databuilder
    environment:
      - AGENT_NAME=vlm_databuilder
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
