version: '3.8'

services:
  speech-to-code:
    build: .
    container_name: speech-to-code
    environment:
      - AGENT_NAME=speech-to-code
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
