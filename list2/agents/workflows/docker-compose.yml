version: '3.8'

services:
  workflows:
    build: .
    container_name: workflows
    environment:
      - AGENT_NAME=workflows
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
