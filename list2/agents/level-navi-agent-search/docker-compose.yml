version: '3.8'

services:
  level-navi-agent-search:
    build: .
    container_name: level-navi-agent-search
    environment:
      - AGENT_NAME=level-navi-agent-search
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
