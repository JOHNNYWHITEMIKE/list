version: '3.8'

services:
  outlines:
    build: .
    container_name: outlines
    environment:
      - AGENT_NAME=outlines
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
