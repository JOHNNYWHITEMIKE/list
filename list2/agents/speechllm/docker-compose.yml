version: '3.8'

services:
  speechllm:
    build: .
    container_name: speechllm
    environment:
      - AGENT_NAME=speechllm
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
