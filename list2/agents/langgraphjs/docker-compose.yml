version: '3.8'

services:
  langgraphjs:
    build: .
    container_name: langgraphjs
    environment:
      - AGENT_NAME=langgraphjs
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
