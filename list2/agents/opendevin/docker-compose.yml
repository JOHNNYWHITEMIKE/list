version: '3.8'

services:
  opendevin:
    build: .
    container_name: opendevin
    environment:
      - AGENT_NAME=opendevin
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
