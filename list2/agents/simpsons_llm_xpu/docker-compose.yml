version: '3.8'

services:
  simpsons_llm_xpu:
    build: .
    container_name: simpsons_llm_xpu
    environment:
      - AGENT_NAME=simpsons_llm_xpu
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
