version: '3.8'

services:
  llm-chain:
    build: .
    container_name: llm-chain
    environment:
      - AGENT_NAME=llm-chain
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
