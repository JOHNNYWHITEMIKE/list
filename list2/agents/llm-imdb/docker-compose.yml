version: '3.8'

services:
  llm-imdb:
    build: .
    container_name: llm-imdb
    environment:
      - AGENT_NAME=llm-imdb
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
