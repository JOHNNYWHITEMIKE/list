version: '3.8'

services:
  nexus:
    build: .
    container_name: nexus
    environment:
      - AGENT_NAME=nexus
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
