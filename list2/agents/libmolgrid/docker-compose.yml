version: '3.8'

services:
  libmolgrid:
    build: .
    container_name: libmolgrid
    environment:
      - AGENT_NAME=libmolgrid
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
