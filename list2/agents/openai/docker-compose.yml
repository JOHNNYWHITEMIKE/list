version: '3.8'

services:
  openai:
    build: .
    container_name: openai
    environment:
      - AGENT_NAME=openai
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
