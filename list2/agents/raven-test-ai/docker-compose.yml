version: '3.8'

services:
  raven-test-ai:
    build: .
    container_name: raven-test-ai
    environment:
      - AGENT_NAME=raven-test-ai
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
