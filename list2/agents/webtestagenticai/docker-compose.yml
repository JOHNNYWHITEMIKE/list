version: '3.8'

services:
  webtestagenticai:
    build: .
    container_name: webtestagenticai
    environment:
      - AGENT_NAME=webtestagenticai
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
