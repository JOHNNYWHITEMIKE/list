version: '3.8'

services:
  swiftcurrent:
    build: .
    container_name: swiftcurrent
    environment:
      - AGENT_NAME=swiftcurrent
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
