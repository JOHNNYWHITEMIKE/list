version: '3.8'

services:
  llm-tools:
    build: .
    container_name: llm-tools
    environment:
      - AGENT_NAME=llm-tools
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
