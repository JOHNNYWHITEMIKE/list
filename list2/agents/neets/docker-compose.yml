version: '3.8'

services:
  neets:
    build: .
    container_name: neets
    environment:
      - AGENT_NAME=neets
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
