version: '3.8'

services:
  tavily:
    build: .
    container_name: tavily
    environment:
      - AGENT_NAME=tavily
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
