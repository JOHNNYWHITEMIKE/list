version: '3.8'

services:
  owasp-agentic-ai:
    build: .
    container_name: owasp-agentic-ai
    environment:
      - AGENT_NAME=owasp-agentic-ai
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
