version: '3.8'

services:
  with:
    build: .
    container_name: with
    environment:
      - AGENT_NAME=with
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
