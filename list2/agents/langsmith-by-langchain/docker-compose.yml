version: '3.8'

services:
  langsmith-by-langchain:
    build: .
    container_name: langsmith-by-langchain
    environment:
      - AGENT_NAME=langsmith-by-langchain
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
