version: '3.8'

services:
  tfc-workflows-tooling:
    build: .
    container_name: tfc-workflows-tooling
    environment:
      - AGENT_NAME=tfc-workflows-tooling
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
