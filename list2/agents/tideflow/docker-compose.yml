version: '3.8'

services:
  tideflow:
    build: .
    container_name: tideflow
    environment:
      - AGENT_NAME=tideflow
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
