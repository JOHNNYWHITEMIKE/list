version: '3.8'

services:
  macllm:
    build: .
    container_name: macllm
    environment:
      - AGENT_NAME=macllm
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
