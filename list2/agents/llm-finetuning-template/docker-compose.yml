version: '3.8'

services:
  llm-finetuning-template:
    build: .
    container_name: llm-finetuning-template
    environment:
      - AGENT_NAME=llm-finetuning-template
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
