version: '3.8'

services:
  pinokio:
    build: .
    container_name: pinokio
    environment:
      - AGENT_NAME=pinokio
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
