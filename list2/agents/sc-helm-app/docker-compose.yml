version: '3.8'

services:
  sc-helm-app:
    build: .
    container_name: sc-helm-app
    environment:
      - AGENT_NAME=sc-helm-app
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
