version: '3.8'

services:
  prompt-eng-ollama-interactive-tutorial:
    build: .
    container_name: prompt-eng-ollama-interactive-tutorial
    environment:
      - AGENT_NAME=prompt-eng-ollama-interactive-tutorial
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
