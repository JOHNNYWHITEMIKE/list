version: '3.8'

services:
  mazerunner:
    build: .
    container_name: mazerunner
    environment:
      - AGENT_NAME=mazerunner
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
