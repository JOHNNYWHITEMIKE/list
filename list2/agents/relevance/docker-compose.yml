version: '3.8'

services:
  relevance:
    build: .
    container_name: relevance
    environment:
      - AGENT_NAME=relevance
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
