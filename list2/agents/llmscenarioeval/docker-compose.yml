version: '3.8'

services:
  llmscenarioeval:
    build: .
    container_name: llmscenarioeval
    environment:
      - AGENT_NAME=llmscenarioeval
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
