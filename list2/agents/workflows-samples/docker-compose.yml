version: '3.8'

services:
  workflows-samples:
    build: .
    container_name: workflows-samples
    environment:
      - AGENT_NAME=workflows-samples
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
