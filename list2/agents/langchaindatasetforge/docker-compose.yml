version: '3.8'

services:
  langchaindatasetforge:
    build: .
    container_name: langchaindatasetforge
    environment:
      - AGENT_NAME=langchaindatasetforge
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
