version: '3.8'

services:
  llm-tpu:
    build: .
    container_name: llm-tpu
    environment:
      - AGENT_NAME=llm-tpu
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
