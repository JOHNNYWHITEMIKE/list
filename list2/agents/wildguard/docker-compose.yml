version: '3.8'

services:
  wildguard:
    build: .
    container_name: wildguard
    environment:
      - AGENT_NAME=wildguard
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
