version: '3.8'

services:
  rageval:
    build: .
    container_name: rageval
    environment:
      - AGENT_NAME=rageval
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
