version: '3.8'

services:
  llm-adapters:
    build: .
    container_name: llm-adapters
    environment:
      - AGENT_NAME=llm-adapters
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
