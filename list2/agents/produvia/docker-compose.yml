version: '3.8'

services:
  produvia:
    build: .
    container_name: produvia
    environment:
      - AGENT_NAME=produvia
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
