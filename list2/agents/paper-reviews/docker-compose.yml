version: '3.8'

services:
  paper-reviews:
    build: .
    container_name: paper-reviews
    environment:
      - AGENT_NAME=paper-reviews
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
