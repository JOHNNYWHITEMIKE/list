version: '3.8'

services:
  typechat:
    build: .
    container_name: typechat
    environment:
      - AGENT_NAME=typechat
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
