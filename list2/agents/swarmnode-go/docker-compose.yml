version: '3.8'

services:
  swarmnode-go:
    build: .
    container_name: swarmnode-go
    environment:
      - AGENT_NAME=swarmnode-go
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
