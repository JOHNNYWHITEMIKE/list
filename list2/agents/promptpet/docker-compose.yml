version: '3.8'

services:
  promptpet:
    build: .
    container_name: promptpet
    environment:
      - AGENT_NAME=promptpet
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
