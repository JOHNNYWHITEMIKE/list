version: '3.8'

services:
  prompt-engineering-notebook:
    build: .
    container_name: prompt-engineering-notebook
    environment:
      - AGENT_NAME=prompt-engineering-notebook
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
