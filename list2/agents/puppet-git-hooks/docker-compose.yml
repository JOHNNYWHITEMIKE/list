version: '3.8'

services:
  puppet-git-hooks:
    build: .
    container_name: puppet-git-hooks
    environment:
      - AGENT_NAME=puppet-git-hooks
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
