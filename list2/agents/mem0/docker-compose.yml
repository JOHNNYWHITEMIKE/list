version: '3.8'

services:
  mem0:
    build: .
    container_name: mem0
    environment:
      - AGENT_NAME=mem0
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
