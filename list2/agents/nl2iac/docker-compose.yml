version: '3.8'

services:
  nl2iac:
    build: .
    container_name: nl2iac
    environment:
      - AGENT_NAME=nl2iac
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
