version: '3.8'

services:
  langchain-js-tools:
    build: .
    container_name: langchain-js-tools
    environment:
      - AGENT_NAME=langchain-js-tools
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
