version: '3.8'

services:
  openpipe:
    build: .
    container_name: openpipe
    environment:
      - AGENT_NAME=openpipe
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
