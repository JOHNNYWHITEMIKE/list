version: '3.8'

services:
  smriprep:
    build: .
    container_name: smriprep
    environment:
      - AGENT_NAME=smriprep
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
