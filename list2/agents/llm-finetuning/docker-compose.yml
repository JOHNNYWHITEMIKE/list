version: '3.8'

services:
  llm-finetuning:
    build: .
    container_name: llm-finetuning
    environment:
      - AGENT_NAME=llm-finetuning
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
