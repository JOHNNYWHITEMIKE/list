version: '3.8'

services:
  tanukipy:
    build: .
    container_name: tanukipy
    environment:
      - AGENT_NAME=tanukipy
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
