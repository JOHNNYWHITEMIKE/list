version: '3.8'

services:
  theagentsgameai:
    build: .
    container_name: theagentsgameai
    environment:
      - AGENT_NAME=theagentsgameai
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
