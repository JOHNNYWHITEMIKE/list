version: '3.8'

services:
  modelscope-agent:
    build: .
    container_name: modelscope-agent
    environment:
      - AGENT_NAME=modelscope-agent
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
