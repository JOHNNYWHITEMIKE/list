version: '3.8'

services:
  tangelo:
    build: .
    container_name: tangelo
    environment:
      - AGENT_NAME=tangelo
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
