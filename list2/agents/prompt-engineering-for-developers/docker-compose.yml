version: '3.8'

services:
  prompt-engineering-for-developers:
    build: .
    container_name: prompt-engineering-for-developers
    environment:
      - AGENT_NAME=prompt-engineering-for-developers
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
