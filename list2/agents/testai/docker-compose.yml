version: '3.8'

services:
  testai:
    build: .
    container_name: testai
    environment:
      - AGENT_NAME=testai
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
