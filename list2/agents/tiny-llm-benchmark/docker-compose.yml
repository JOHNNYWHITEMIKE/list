version: '3.8'

services:
  tiny-llm-benchmark:
    build: .
    container_name: tiny-llm-benchmark
    environment:
      - AGENT_NAME=tiny-llm-benchmark
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
