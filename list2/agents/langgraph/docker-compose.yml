version: '3.8'

services:
  langgraph:
    build: .
    container_name: langgraph
    environment:
      - AGENT_NAME=langgraph
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
