version: '3.8'

services:
  tabby:
    build: .
    container_name: tabby
    environment:
      - AGENT_NAME=tabby
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
