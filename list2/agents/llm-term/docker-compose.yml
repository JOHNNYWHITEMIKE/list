version: '3.8'

services:
  llm-term:
    build: .
    container_name: llm-term
    environment:
      - AGENT_NAME=llm-term
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
