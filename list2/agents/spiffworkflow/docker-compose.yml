version: '3.8'

services:
  spiffworkflow:
    build: .
    container_name: spiffworkflow
    environment:
      - AGENT_NAME=spiffworkflow
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
