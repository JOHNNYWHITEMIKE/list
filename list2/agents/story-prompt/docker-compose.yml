version: '3.8'

services:
  story-prompt:
    build: .
    container_name: story-prompt
    environment:
      - AGENT_NAME=story-prompt
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
