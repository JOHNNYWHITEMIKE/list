version: '3.8'

services:
  smartcall-ai:
    build: .
    container_name: smartcall-ai
    environment:
      - AGENT_NAME=smartcall-ai
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
