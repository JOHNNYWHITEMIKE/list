version: '3.8'

services:
  ramp-workflow:
    build: .
    container_name: ramp-workflow
    environment:
      - AGENT_NAME=ramp-workflow
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
