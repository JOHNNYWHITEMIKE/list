version: '3.8'

services:
  swarmnode-dotnet:
    build: .
    container_name: swarmnode-dotnet
    environment:
      - AGENT_NAME=swarmnode-dotnet
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
