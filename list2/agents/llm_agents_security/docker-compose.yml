version: '3.8'

services:
  llm_agents_security:
    build: .
    container_name: llm_agents_security
    environment:
      - AGENT_NAME=llm_agents_security
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
