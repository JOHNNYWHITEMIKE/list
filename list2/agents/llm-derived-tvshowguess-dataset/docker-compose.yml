version: '3.8'

services:
  llm-derived-tvshowguess-dataset:
    build: .
    container_name: llm-derived-tvshowguess-dataset
    environment:
      - AGENT_NAME=llm-derived-tvshowguess-dataset
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
