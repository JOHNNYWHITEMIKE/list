version: '3.8'

services:
  tortoise-tts:
    build: .
    container_name: tortoise-tts
    environment:
      - AGENT_NAME=tortoise-tts
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
