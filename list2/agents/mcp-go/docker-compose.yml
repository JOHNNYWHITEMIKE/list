version: '3.8'

services:
  mcp-go:
    build: .
    container_name: mcp-go
    environment:
      - AGENT_NAME=mcp-go
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
