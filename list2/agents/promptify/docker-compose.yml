version: '3.8'

services:
  promptify:
    build: .
    container_name: promptify
    environment:
      - AGENT_NAME=promptify
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
