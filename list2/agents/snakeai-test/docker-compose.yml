version: '3.8'

services:
  snakeai-test:
    build: .
    container_name: snakeai-test
    environment:
      - AGENT_NAME=snakeai-test
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
