version: '3.8'

services:
  shimmering-obsidian:
    build: .
    container_name: shimmering-obsidian
    environment:
      - AGENT_NAME=shimmering-obsidian
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
