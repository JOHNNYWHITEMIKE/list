version: '3.8'

services:
  t2t-polish:
    build: .
    container_name: t2t-polish
    environment:
      - AGENT_NAME=t2t-polish
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
