version: '3.8'

services:
  worldwide_ai-ethics:
    build: .
    container_name: worldwide_ai-ethics
    environment:
      - AGENT_NAME=worldwide_ai-ethics
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
