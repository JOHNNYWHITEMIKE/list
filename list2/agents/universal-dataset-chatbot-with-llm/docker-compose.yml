version: '3.8'

services:
  universal-dataset-chatbot-with-llm:
    build: .
    container_name: universal-dataset-chatbot-with-llm
    environment:
      - AGENT_NAME=universal-dataset-chatbot-with-llm
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
