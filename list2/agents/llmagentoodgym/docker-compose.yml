version: '3.8'

services:
  llmagentoodgym:
    build: .
    container_name: llmagentoodgym
    environment:
      - AGENT_NAME=llmagentoodgym
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
