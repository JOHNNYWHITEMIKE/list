version: '3.8'

services:
  swarmnode-rust:
    build: .
    container_name: swarmnode-rust
    environment:
      - AGENT_NAME=swarmnode-rust
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
