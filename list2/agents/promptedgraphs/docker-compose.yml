version: '3.8'

services:
  promptedgraphs:
    build: .
    container_name: promptedgraphs
    environment:
      - AGENT_NAME=promptedgraphs
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
