version: '3.8'

services:
  super-table:
    build: .
    container_name: super-table
    environment:
      - AGENT_NAME=super-table
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
