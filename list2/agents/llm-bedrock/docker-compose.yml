version: '3.8'

services:
  llm-bedrock:
    build: .
    container_name: llm-bedrock
    environment:
      - AGENT_NAME=llm-bedrock
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
