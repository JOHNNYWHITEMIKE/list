version: '3.8'

services:
  llm_optimize:
    build: .
    container_name: llm_optimize
    environment:
      - AGENT_NAME=llm_optimize
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
