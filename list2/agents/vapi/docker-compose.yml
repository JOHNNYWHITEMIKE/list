version: '3.8'

services:
  vapi:
    build: .
    container_name: vapi
    environment:
      - AGENT_NAME=vapi
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
