version: '3.8'

services:
  lawful-good:
    build: .
    container_name: lawful-good
    environment:
      - AGENT_NAME=lawful-good
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
