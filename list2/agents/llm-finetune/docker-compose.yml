version: '3.8'

services:
  llm-finetune:
    build: .
    container_name: llm-finetune
    environment:
      - AGENT_NAME=llm-finetune
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
