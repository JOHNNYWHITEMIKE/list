version: '3.8'

services:
  promptbase:
    build: .
    container_name: promptbase
    environment:
      - AGENT_NAME=promptbase
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
