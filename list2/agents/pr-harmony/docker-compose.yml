version: '3.8'

services:
  pr-harmony:
    build: .
    container_name: pr-harmony
    environment:
      - AGENT_NAME=pr-harmony
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
