version: '3.8'

services:
  venture:
    build: .
    container_name: venture
    environment:
      - AGENT_NAME=venture
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
