version: '3.8'

services:
  mlx-llm:
    build: .
    container_name: mlx-llm
    environment:
      - AGENT_NAME=mlx-llm
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
