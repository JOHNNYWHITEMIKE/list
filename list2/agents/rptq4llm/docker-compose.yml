version: '3.8'

services:
  rptq4llm:
    build: .
    container_name: rptq4llm
    environment:
      - AGENT_NAME=rptq4llm
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
