version: '3.8'

services:
  promptagent:
    build: .
    container_name: promptagent
    environment:
      - AGENT_NAME=promptagent
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
