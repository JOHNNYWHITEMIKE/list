version: '3.8'

services:
  zeus-llm-trainer:
    build: .
    container_name: zeus-llm-trainer
    environment:
      - AGENT_NAME=zeus-llm-trainer
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
