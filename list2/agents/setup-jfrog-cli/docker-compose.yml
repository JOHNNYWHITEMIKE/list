version: '3.8'

services:
  setup-jfrog-cli:
    build: .
    container_name: setup-jfrog-cli
    environment:
      - AGENT_NAME=setup-jfrog-cli
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
