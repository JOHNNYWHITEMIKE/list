version: '3.8'

services:
  languagemodels:
    build: .
    container_name: languagemodels
    environment:
      - AGENT_NAME=languagemodels
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
