version: '3.8'

services:
  swarmnode-python:
    build: .
    container_name: swarmnode-python
    environment:
      - AGENT_NAME=swarmnode-python
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
