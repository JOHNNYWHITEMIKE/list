version: '3.8'

services:
  langchainbitcoin:
    build: .
    container_name: langchainbitcoin
    environment:
      - AGENT_NAME=langchainbitcoin
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
