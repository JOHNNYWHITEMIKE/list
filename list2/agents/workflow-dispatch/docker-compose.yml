version: '3.8'

services:
  workflow-dispatch:
    build: .
    container_name: workflow-dispatch
    environment:
      - AGENT_NAME=workflow-dispatch
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
