version: '3.8'

services:
  langserve:
    build: .
    container_name: langserve
    environment:
      - AGENT_NAME=langserve
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
