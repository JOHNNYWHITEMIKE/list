version: '3.8'

services:
  toolqa:
    build: .
    container_name: toolqa
    environment:
      - AGENT_NAME=toolqa
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
