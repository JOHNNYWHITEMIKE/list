version: '3.8'

services:
  structgenius:
    build: .
    container_name: structgenius
    environment:
      - AGENT_NAME=structgenius
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
