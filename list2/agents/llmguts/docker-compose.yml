version: '3.8'

services:
  llmguts:
    build: .
    container_name: llmguts
    environment:
      - AGENT_NAME=llmguts
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
