version: '3.8'

services:
  pydantic:
    build: .
    container_name: pydantic
    environment:
      - AGENT_NAME=pydantic
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
