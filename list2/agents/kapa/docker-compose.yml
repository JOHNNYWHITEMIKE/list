version: '3.8'

services:
  kapa:
    build: .
    container_name: kapa
    environment:
      - AGENT_NAME=kapa
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
