version: '3.8'

services:
  starjob:
    build: .
    container_name: starjob
    environment:
      - AGENT_NAME=starjob
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
