version: '3.8'

services:
  teamx:
    build: .
    container_name: teamx
    environment:
      - AGENT_NAME=teamx
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
