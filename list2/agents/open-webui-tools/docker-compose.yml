version: '3.8'

services:
  open-webui-tools:
    build: .
    container_name: open-webui-tools
    environment:
      - AGENT_NAME=open-webui-tools
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
