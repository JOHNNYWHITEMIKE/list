version: '3.8'

services:
  sdp-llm-dataset:
    build: .
    container_name: sdp-llm-dataset
    environment:
      - AGENT_NAME=sdp-llm-dataset
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
