version: '3.8'

services:
  llm_tools:
    build: .
    container_name: llm_tools
    environment:
      - AGENT_NAME=llm_tools
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
