version: '3.8'

services:
  multimodal-llm-agent:
    build: .
    container_name: multimodal-llm-agent
    environment:
      - AGENT_NAME=multimodal-llm-agent
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
